/**
 * Online OpenRocket user guide content — the in-app rendered version of
 * docs/user-guide.md. Generated by the documentation workflow; each section's
 * `html` is our own trusted, static markup (never user input), rendered via
 * dangerouslySetInnerHTML in GuideDialog.
 *
 * To regenerate: re-run the user-guide workflow and rebuild GUIDE_SECTIONS.
 */

export interface GuideSection {
  id: string;
  title: string;
  /** Trusted static HTML (semantic tags only, no scripts/styles). */
  html: string;
}

export const GUIDE_SECTIONS: GuideSection[] = [
  {
    "id": "welcome",
    "title": "Welcome to Online OpenRocket",
    "html": "<p>Online OpenRocket is the real OpenRocket flight simulator, running entirely in your web browser. There is nothing to install: the same <strong>24.12 physics kernel</strong> that powers the OpenRocket desktop application has been compiled to JavaScript and verified bit-for-bit against the desktop program. When your browser reports an apogee, it ran the same code, in the same order, with the same rounding as the desktop app.</p>\n<p>The workflow is simple and always the same. You design a rocket by building up its components, hang a motor, set your launch conditions, and press <strong>Launch</strong> to see how high it flies, how fast it goes, and whether it will fly straight and land safely. This guide is written for model and high-power rocketry hobbyists at every level, from your first Estes kit to a multi-stage high-power project.</p>\n<p>The guide has three parts:</p>\n<ul>\n<li><strong>Quick Start</strong> gets you to your first successful flight in about a minute.</li>\n<li><strong>The In-Depth Feature Guide</strong> is the complete reference for everything the app can do &mdash; designing, motors, launch conditions, simulation, staging, files, and preferences.</li>\n<li><strong>How It Works</strong> opens up the physics, the math, and the references behind the numbers, so you can decide exactly how far to trust them.</li>\n</ul>"
  },
  {
    "id": "quick-start",
    "title": "Quick Start",
    "html": "<p>This section gets you to your first successful flight in about a minute. The rest of the guide then covers everything in depth.</p>\n<h2>Your first flight in three steps</h2>\n<p>When the app opens, you already have a complete, flyable rocket called <strong>My Rocket</strong> &mdash; a nose cone, a body tube, a fin set, a motor mount, and a parachute. You don't have to build anything to get started. All it's missing is a motor.</p>\n<p><strong>1. Load a motor.</strong> In the left sidebar, find the <strong>Motors</strong> panel. The fastest option is the <strong>Quick picks (built-in, offline)</strong> dropdown &mdash; pick a classic Estes motor like <strong>B6-4</strong> or <strong>C6-5</strong> and it loads instantly, no download needed. (The number after the dash is the ejection delay in seconds.) Want something specific? Click <strong>&#128270; Browse motor database&hellip;</strong> to search the full thrustcurve.org catalog of about 1,100 motors &mdash; but for your first flight, a quick pick is all you need.</p>\n<p><strong>2. Check your launch conditions (optional).</strong> Below the Motors panel is the <strong>Launch conditions</strong> panel. The defaults are sensible: a 1-meter launch rod, pointed straight up, no wind, standard sea-level atmosphere. You can leave every field alone for now.</p>\n<p><strong>3. Press Launch.</strong> Hit the <strong>Launch</strong> button at the bottom of the Launch conditions panel. The simulation runs in a fraction of a second and your results appear in the main area.</p>\n<p>That's it &mdash; you've flown a rocket.</p>\n<h2>Reading your first result</h2>\n<p>After launching, the headline tiles across the top of the results give you the numbers that matter most:</p>\n<ul>\n<li><strong>Apogee</strong> &mdash; the peak altitude the rocket reached. This is your \"how high did it go\" number.</li>\n<li><strong>Max velocity</strong> &mdash; the fastest the rocket traveled (also shown as <strong>Max Mach</strong> in the detailed report).</li>\n<li><strong>Max accel</strong> &mdash; peak acceleration, a feel for how hard it leaves the pad.</li>\n<li><strong>Apogee at</strong> &mdash; seconds from liftoff to the top of the flight.</li>\n<li><strong>Descent hits</strong> / <strong>Flight time</strong> &mdash; how fast it's coming down at landing and how long the whole flight lasted.</li>\n</ul>\n<p>Two health checks tell you whether the flight is actually <em>safe and stable</em>, not just how high it went:</p>\n<ul>\n<li><strong>Stability margin</strong> (shown in the design stats as <strong>Stability</strong>, in <em>calibers</em>). A healthy rocket sits between <strong>1.0 and 3.0 cal</strong>. Below 1.0 the rocket is <em>under-stable</em> and may wobble or go unstable &mdash; flagged with a &#9888;. Above 3.0 it's <em>over-stable</em> and tends to weathercock (turn into the wind). Right in that band, marked &#10003;, is what you want.</li>\n<li><strong>Landing descent rate</strong> &mdash; you want to touch down at <strong>20 ft/s (about 6 m/s) or slower</strong> so nothing breaks. The report flags a faster landing.</li>\n</ul>\n<p>Open the full launch report below the tiles for more: <strong>thrust-to-weight at the rod</strong> (aim for at least <strong>5:1</strong>), <strong>rod-exit speed</strong> (at least about 50 ft/s / 15 m/s so the fins have airflow to steer with), deployment timing, and plain-language safety comments that name any problem and where it is.</p>\n<p>If your first rocket flies stable and lands soft &mdash; congratulations, you're done. If a value comes up flagged, the report tells you which one and why, so you know what to adjust.</p>\n<h2>Where to go next</h2>\n<p>Now that you've seen a flight end to end, the rest of this guide shows you how to make it <em>your</em> rocket:</p>\n<ul>\n<li><strong>Build and edit components</strong> &mdash; reshape the nose, resize the body, design custom fins (including freeform), and add stages using the component tree.</li>\n<li><strong>Choose the right motor</strong> &mdash; filter the full database by manufacturer, diameter, and impulse; drill ejection delays to any whole second; and batch-test many motors at once.</li>\n<li><strong>Dial in launch conditions</strong> &mdash; wind, launch angle, site altitude, and weather.</li>\n<li><strong>Read the plots and reports</strong> &mdash; altitude, velocity, and stability over the whole flight, plus saving and exporting your results.</li>\n<li><strong>Import and export</strong> &mdash; load and save <code>.ork</code>, RockSim, and RASAero files.</li>\n</ul>"
  },
  {
    "id": "designing-the-rocket",
    "title": "Designing the Rocket",
    "html": "<p>Everything below is organized by the order you actually work: design the rocket, look at it, hang a motor, set the weather, fly it, read the results, then save or export. This section covers the design stage.</p>\n<h2>The component tree</h2>\n<p>The left editor is a <strong>stage-rooted tree</strong> of components. Each stage holds an axial chain of external body parts (nose cone &rarr; body tubes &rarr; transitions), and those in turn hold internal parts (fins, motor mounts, recovery gear, mass). Click a node to select it and open its property panel; inline buttons on each node <strong>move, duplicate, or delete</strong> it. The tree enforces OpenRocket's containment rules &mdash; you can only add children a parent legally accepts (for example, fins and inner tubes go inside a body tube, a parachute goes inside any body part, and an engine block goes inside an inner tube).</p>\n<h2>The seventeen component types</h2>\n<div>\n<table>\n<thead><tr><th>Type</th><th>Category</th><th>Key parameters</th></tr></thead>\n<tbody>\n<tr><td>Nose cone</td><td>External body</td><td>length, base radius, wall thickness, shape, shoulder</td></tr>\n<tr><td>Transition</td><td>External body</td><td>length, fore/aft radius, thickness, shape, two shoulders</td></tr>\n<tr><td>Body tube</td><td>External body</td><td>length, outer radius, thickness</td></tr>\n<tr><td>Trapezoidal fins</td><td>Fin set</td><td>count, root/tip chord, sweep, height, thickness, cant, cross-section, tabs</td></tr>\n<tr><td>Elliptical fins</td><td>Fin set</td><td>count, root chord, height, thickness, cross-section, tabs</td></tr>\n<tr><td>Freeform fins</td><td>Fin set</td><td>count, thickness, cross-section, tabs, polygon outline</td></tr>\n<tr><td>Tube fins</td><td>Fin set</td><td>count (up to 12), length, outer radius</td></tr>\n<tr><td>Inner tube</td><td>Structure / mount</td><td>length, outer radius, thickness, cluster layout</td></tr>\n<tr><td>Tube coupler</td><td>Structure</td><td>length, thickness</td></tr>\n<tr><td>Centering ring</td><td>Structure</td><td>axial thickness</td></tr>\n<tr><td>Bulkhead</td><td>Structure</td><td>axial thickness</td></tr>\n<tr><td>Engine block</td><td>Structure</td><td>length, thickness</td></tr>\n<tr><td>Launch lug</td><td>Guide</td><td>length, outer radius, thickness</td></tr>\n<tr><td>Rail button</td><td>Guide</td><td>outer diameter</td></tr>\n<tr><td>Parachute</td><td>Recovery</td><td>canopy diameter, Cd, lines, deploy event</td></tr>\n<tr><td>Streamer</td><td>Recovery</td><td>strip length/width, Cd, deploy event</td></tr>\n<tr><td>Shock cord</td><td>Recovery</td><td>cord length</td></tr>\n<tr><td>Mass component</td><td>Ballast</td><td>mass, length, radius</td></tr>\n</tbody>\n</table>\n</div>\n<h2>Dimensions: diameter vs. radius</h2>\n<p>Internally every size is stored in <strong>pure SI</strong> (meters, kilograms, seconds) and angles in radians. The property panel shows friendlier units (mm, degrees, grams) and converts at the edge. Fields that describe a round cross-section are stored as a <strong>radius</strong>, but if you set your preference to \"Diameter\" the panel doubles the value and relabels it &mdash; so \"Base outer radius\" becomes \"Base outer diameter\" and accepts the tube's full OD. This matters because catalog tubes are always quoted by diameter, so most builders switch to diameter mode.</p>\n<h2>Nose cones and transitions</h2>\n<p>Both support six mathematical <strong>shapes</strong>: Ogive, Conical, Ellipsoid, Parabolic, Haack, and Power. Shapes with a free parameter fall back to OpenRocket's own defaults (ogive/parabolic = 1, power = 0.5) so a missing parameter never silently reshapes the part. A nose cone can be <strong>solid (filled)</strong> or a hollow shell of a given wall thickness, and can carry a <strong>shoulder</strong> (radius, length, thickness, and an optional end cap) that plugs into the tube below. A transition is a shoulder-to-shoulder part with independent fore and aft radii plus a shoulder on <strong>each</strong> end &mdash; use it as a reducer, boat tail, or coupler flare.</p>\n<h2>Fins</h2>\n<p>Four fin families are supported:</p>\n<ul>\n<li><strong>Trapezoidal</strong> &mdash; the everyday fin, defined by root chord, tip chord, sweep, and height.</li>\n<li><strong>Elliptical</strong> &mdash; a rounded planform from root chord and height.</li>\n<li><strong>Freeform</strong> &mdash; an arbitrary polygon. Selecting one opens the <strong>freeform point editor</strong>, where you drag vertices to draw any outline (needs at least three points).</li>\n<li><strong>Tube fins</strong> &mdash; a ring of full tubes instead of flat fins (up to 12).</li>\n</ul>\n<p>Every flat-fin set also carries a <strong>cant angle</strong> (to induce roll), a <strong>fin count</strong> (1&ndash;8), and a <strong>cross-section</strong>: <strong>Square</strong>, <strong>Rounded</strong>, or <strong>Airfoil (pointed)</strong>. The cross-section is not cosmetic &mdash; a square edge produces markedly more pressure/skin drag than a streamlined airfoil, and the kernel models that difference, so choosing \"Airfoil\" on a carefully sanded fin gives you the lower drag you actually built.</p>\n<p><strong>Through-the-wall tabs</strong>: a tab exists only when <strong>both</strong> its depth and length are greater than zero (OpenRocket's rule). You set the tab depth, length, offset, and the reference edge the offset is measured from (front / middle / end of fin). The engine clamps tab depth to the body radius, and the tab's volume counts toward fin mass and CG.</p>\n<h2>Positioning and snapping</h2>\n<p>Internal and fin components are placed <strong>axially</strong> relative to their parent, using a method (top / middle / bottom / absolute) plus an offset. You can drag parts in the 2D schematic or nudge the offset slider. The editor provides <strong>magnetic snapping</strong> to meaningful anchors: the parent's ends and middle, every sibling's leading and trailing edges, and &mdash; importantly &mdash; the front and rear edges of a fin's tab, because in a real build the centering rings butt against the tab where it passes through the wall. Any \"absolute\" positions from an imported file are rewritten to parent-relative offsets so the drawn geometry always matches the simulated geometry.</p>\n<h2>Motor mounts and clusters</h2>\n<p>A motor mount is modeled as an <strong>inner tube</strong> with the mount flag set. (If you import a file with a motor stuck directly in a body tube, the importer tells you to move it onto an inner tube.) An inner tube also carries a <strong>cluster layout</strong> &mdash; single, rows, rings, and stars up to nine motors, with a spacing multiplier (&times; tube diameter) and a rotation. One motor choice serves the whole cluster: thrust is multiplied by the count and mass is placed at the real tube positions, exactly matching the kernel's ClusterConfiguration.</p>\n<h2>Recovery</h2>\n<p><strong>Parachutes</strong> take a canopy diameter, a drag coefficient (blank = automatic, defaulting to 0.8 after deployment), line count, and line length. <strong>Streamers</strong> take strip length and width. Both carry a <strong>deployment trigger</strong>: motor ejection charge, apogee, altitude (descending, with an AGL altitude), launch, or never &mdash; plus a deploy delay. A <strong>shock cord</strong> and a <strong>mass component</strong> (for nose weight, altimeters, or dead ballast) round out the bay.</p>\n<h2>Mass, CG, and Cd overrides</h2>\n<p>Any component can override its computed <strong>mass</strong>, <strong>axial CG position</strong>, or <strong>drag coefficient</strong>. This is how you tell the sim what a part <em>actually</em> weighs when the geometry can't know &mdash; a machined coupler, a loaded electronics bay, a store-bought chute. Applying a component preset that carries a cataloged mass sets a mass override automatically, because a real part weighs what it weighs, not what its shell computes.</p>\n<h2>Materials, finishes, and color</h2>\n<p>Solid and structural parts pick a <strong>bulk material</strong> from the desktop material database (writing its density); parachutes and streamers pick a <strong>surface material</strong>, and shock cords/lines pick a <strong>line material</strong>. Typing a custom density detaches the part from the named material. Each body part also has a <strong>surface finish</strong> &mdash; Rough, Unfinished, Regular paint, Smooth paint, Aircraft sheet-metal, or Polished &mdash; and surface roughness feeds the skin-friction drag model, so a polished airframe genuinely simulates lower drag. Finally, each component has a <strong>display color</strong> (a full picker plus one-click presets) used only in the 2D/3D views.</p>\n<h2>Component presets</h2>\n<p>The bundled catalog holds roughly <strong>4,700 real-world parts</strong> (tubes, nose cones, transitions, rings, couplers, bulkheads, engine blocks, launch lugs, parachutes, streamers) from named manufacturers with part numbers, materials, and cataloged mass. Open the preset picker on a selected component, choose a part, and its dimensions, material, and mass override are patched in. The 1.3 MB bundle loads lazily on first use, and you can round-trip your own custom presets through CSV.</p>"
  },
  {
    "id": "visualizing-the-design",
    "title": "Visualizing the Design",
    "html": "<p>As you build, three views keep the design honest &mdash; a scale side view, a rotatable 3D shell, and printable 1:1 fin templates.</p>\n<h2>The 2D schematic</h2>\n<p>A true-scale side view drawn from the tree. It shows tubes, lathed nose/transition profiles, fins at their real planforms, fin tabs, and motor cases drawn to scale, plus <strong>CG and CP markers</strong> in standard rocketry symbols. You can <strong>drag components</strong> to reposition them (with the snapping described earlier) and pan/zoom.</p>\n<h2>The 3D view</h2>\n<p>A three.js render of the external shell you can <strong>drag to rotate and scroll to zoom</strong>. It marks the <strong>CG</strong> (a neutral sphere) and <strong>CP</strong> (a red sphere) so you can see the stability margin in space.</p>\n<h2>1:1 fin templates (SVG)</h2>\n<p>From any fin set you can export a <strong>true-scale printable cutting template</strong>. It draws the fin outline as a hairline cut path, a dashed root-chord reference line, the through-the-wall tab rectangle where present, a label block (rocket/fin name, cut count, chord/height/thickness/cross-section/tab depth), and a <strong>50 mm calibration ruler</strong>. Print at 100% with no fit-to-page, then check the ruler measures exactly 50 mm &mdash; printers silently rescale, and the ruler is how you catch it. The SVG uses physical millimeter units and a 0.2 mm hairline, so it feeds a laser cutter directly.</p>"
  },
  {
    "id": "motors",
    "title": "Motors",
    "html": "<p>With the airframe drawn, the next step is choosing a motor. The app bundles the full thrustcurve.org catalog metadata and downloads the actual thrust curves on demand.</p>\n<h2>The database and browser</h2>\n<p>The app bundles metadata for about <strong>1,129 thrustcurve.org motors</strong> (designation, manufacturer, impulse class, diameter, length, average/max thrust, total impulse, burn time, loaded and propellant weight, delays, availability, propellant/case info, type). <strong>Thrust curves are not bundled</strong> &mdash; they download on demand the first time you pick a motor and cache in your browser. From a motor mount, click <strong>Browse motor database</strong> to open the browser; the header shows the source, date, and the mount's diameter. When a curve lacks CG data the kernel uses OpenRocket's model: mass drops from loaded to burnout weight in proportion to cumulative impulse, with CG fixed at half length.</p>\n<h2>Filters</h2>\n<p>The browser gives you <strong>manufacturer chips</strong> (with live counts), <strong>diameter-class chips</strong>, an <strong>include out-of-production</strong> checkbox, and free-text search across the designation and common name. The results table (capped at 400 rows) sorts by designation, manufacturer, diameter, length, burn time, or total impulse (the default). Filters persist between sessions. Sorting the \"Motor\" column sorts by the cleaned-up <em>display</em> designation, so a Cesaroni motor like <code>381I224-15A</code> is shown and ordered as <code>I224-15A</code> rather than by its impulse-prefix digits, while the raw designation stays the file identity.</p>\n<h2>Diameter-class fit (adapter-down)</h2>\n<p>The mount's inner diameter drives which classes appear. A mount fits <strong>every class at or below its own</strong> &mdash; smaller motors ride in adapters &mdash; but <strong>never</strong> a larger one. Motors too big are physically excluded from the list, not merely flagged. Note that <strong>75 mm and 76 mm are the same class</strong> (shown \"75/76\"), and diameters snap to the nearest common class within tolerance because tube IDs run oversize.</p>\n<h2>Delays and the drill-to-fit rule</h2>\n<p>The prescribed delays from the motor's label are informational only. In the browser's <strong>Delay</strong> select you can choose Auto (optimal), any prescribed delay, or <strong>Custom</strong> and type a value; on the main Motors panel you can type <strong>any whole-second delay</strong> without reloading, or tick <strong>auto (optimal)</strong> on the sustainer. This is the drill-to-fit rule: real flyers drill an adjustable delay to whatever whole second they want, so the recommendation is <code>round(optimum)</code> and is <strong>never snapped to the prescribed list</strong>. One subtlety: the kernel's optimum delay is the coast from burnout to <em>ballistic</em> apogee (a deployment-free probe), and auto uses a two-pass approach &mdash; fly once, read the optimum, round it, and re-fly.</p>\n<h2>Per-stage maximum motor length</h2>\n<p>Each <strong>stage</strong> has its own \"Max motor length\" &mdash; a booster and a sustainer have different room. Motors longer than the limit are flagged in the browser and excluded from batch simulation. This is separate from the diameter-fit rule; a motor can be the right diameter but too long.</p>\n<h2>EX and custom motors, and built-in quick picks</h2>\n<p>You can <strong>import experimental motors</strong> from RASP <code>.eng</code> or RockSim <code>.rse</code> files. They appear under an \"EX\" manufacturer (the real manufacturer name is preserved), filter and simulate like any database motor, and &mdash; for <code>.rse</code> files with measured per-sample masses &mdash; use those masses instead of the impulse-proportional model. Curves are local; no network needed. For instant offline use there are three <strong>built-in quick picks</strong> (A8-3, B6-4, C6-5) as full motor specs. When you import a design whose motor prefix-matches a built-in, the app loads the built-in curve but <strong>keeps the file's ejection delay</strong>, so a saved C6-7 doesn't silently become a C6-5.</p>"
  },
  {
    "id": "launch-conditions",
    "title": "Launch Conditions",
    "html": "<p>Before you fly, the Launch Conditions panel sets the pad and the weather. It collects eight fields: <strong>launch rod/rail length</strong> (default 1 m), <strong>rod angle</strong> (0&deg;, range &minus;30&hellip;30&deg;), <strong>average wind</strong>, <strong>wind gust sigma</strong> (standard deviation of gusts about the average), <strong>site altitude</strong> (0&ndash;10,000 m), <strong>latitude</strong> (default 28.61&deg;, roughly Florida), <strong>temperature</strong> (blank = ISA standard), and <strong>pressure</strong> (blank = ISA standard). Values display in your preferred units and convert to SI on Launch. When temperature and pressure are blank the sim uses the ISA atmosphere (288.15 K, 101325 Pa, &minus;6.5 K/km lapse).</p>\n<p><strong>Deterministic by design</strong>: wind turbulence is seeded (default seed 42), so identical inputs always produce an identical flight. This is intentional and differs from the desktop's time-seeded randomness &mdash; re-running won't vary the result, and that is not a bug. (The physics behind the wind model and this determinism choice is covered in <em>How It Works</em>.)</p>"
  },
  {
    "id": "simulating-and-results",
    "title": "Simulating and Reading Results",
    "html": "<p>With a motor assigned and conditions set, press <strong>Launch</strong>. This section covers the single-flight run, the plots, the safety-graded report, saved runs, and batch simulation.</p>\n<h2>Running a single flight</h2>\n<p>Assign a motor, set conditions, and press <strong>Launch</strong>. The app builds the flight options and simulates once; if the primary mount is on auto-delay it reads the kernel's optimum, rounds it, sets that delay, and re-flies. The primary mount is the topmost stage's mount that has a motor &mdash; it drives the lead columns and the auto-delay probe. Engine invariants: <strong>RK4 with adaptive time step, Extended Barrowman aerodynamics, quaternion orientation, and a 6DOF&rarr;3DOF switch after recovery deployment.</strong> (One automation caveat: the sim runs inside a requestAnimationFrame callback, which Chrome pauses in hidden tabs &mdash; a stuck \"Simulating\" means the window isn't visible.)</p>\n<h2>Flight plots</h2>\n<p>Up to <strong>eleven synchronized single-series panels</strong>: Altitude, Velocity, Acceleration, Mass, Thrust, Drag force, Mach number, Stability margin (cal), CP location, CG location, and Angle of attack. A chip bar toggles panels (default: altitude/velocity/acceleration), and a series only appears if the kernel actually produced it. Following dataviz discipline, <strong>different-scale measures are never dual-axed</strong> &mdash; each gets its own panel and y-scale, with a fixed per-series color and theme-aware axes. Hover for synchronized crosshair readouts; the <strong>CSV</strong> button exports every populated series, time-aligned.</p>\n<h2>The launch report and safety checks</h2>\n<p>The report header shows the <strong>optimal / recommended(available) / flown</strong> delay, then a per-device <strong>recovery table</strong> (drogue and main each get a row: deploy time, altitude, opening velocity, settled descent rate, and a verdict). \"Show all details\" expands roughly thirty attributes: max altitude/velocity/Mach/acceleration, times to guide departure / burnout / apogee / landing, velocity and thrust:weight at guide departure, launch mass/CG/CP/static margin, landing descent rate, plus the motor's diameter, manufacturer, type, propellant, and case.</p>\n<p>Every flight is graded against these thresholds:</p>\n<div>\n<table>\n<thead><tr><th>Check</th><th>Threshold</th><th>Meaning</th></tr></thead>\n<tbody>\n<tr><td>Lift-off speed</td><td>&ge; 15 m/s (~50 ft/s)</td><td>Enough airspeed leaving the guide to be stable</td></tr>\n<tr><td>Thrust:weight</td><td>&ge; 5:1</td><td>Adequate initial acceleration</td></tr>\n<tr><td>Safe deployment</td><td>&le; 21.34 m/s (70 ft/s)</td><td>Opening shock won't zipper the tube</td></tr>\n<tr><td>Drogue descent</td><td>&le; 21.34 m/s (70 ft/s)</td><td>Top of accepted drogue band</td></tr>\n<tr><td>Landing rate</td><td>&le; 6.1 m/s (20 ft/s)</td><td>Survivable ground hit</td></tr>\n<tr><td>Static margin</td><td>1.0&ndash;3.0 cal</td><td>Stable but not so over-stable it weathercocks</td></tr>\n</tbody>\n</table>\n</div>\n<p>The report also flags <strong>weathercocking risk</strong> and echoes the average wind, so an over-stable rocket in a stiff breeze reads as a warning rather than a silent number.</p>\n<h2>Saved runs and CSV</h2>\n<p>Every flight is stored to a <strong>run history</strong> (up to 500 runs, surviving reloads). A typical flight-day flow is to fly many motors, compare the table, and download it as <strong>CSV</strong>. Note that reopening an old saved run shows its report but not its charts &mdash; charts need a fresh simulation's raw series.</p>\n<h2>Batch simulate</h2>\n<p>Batch mode flies <strong>every motor that fits the mount</strong> (after your manufacturer/class/OOP filters) through the current design at each motor's auto-optimal delay, then grades each flight against your acceptance criteria &mdash; minimum rod-exit velocity, minimum thrust:weight, and an apogee window. Results append to the run history and download as CSV. This is the motor-shopping tool: pick the airframe, pour in a manufacturer's whole catalog, and read off which motors hit your target altitude safely.</p>"
  },
  {
    "id": "multi-stage-and-clusters",
    "title": "Multi-Stage and Clustered Rockets",
    "html": "<p>Once you are comfortable with single-stage flights, the same tools scale up to staged and clustered rockets.</p>\n<h2>Staging and separation</h2>\n<p>Add stages as siblings under the rocket root; they flatten nose-to-tail into one chain for layout. A <strong>lower</strong> stage carries a <strong>separation trigger</strong>: its own ejection charge (the default), its own burnout, its own ignition, the upper stage's ignition, launch, apogee, altitude ascending/descending, or never &mdash; plus a separation delay. The top stage ignores separation (it separates <em>from</em> nothing). After a stage drops, the kernel simulates the separated piece's own flight, which the report can summarize.</p>\n<h2>Ignition and the G80 rule</h2>\n<p>Each staged motor has an <strong>ignition trigger</strong>: Automatic (launch-stage motors at launch, upper motors on the lower stage's ejection charge &mdash; right for low and mid power), lower-stage burnout + delay (electronics), launch + delay (timer), lower-stage ejection charge + delay, or never. The critical default is the <strong>G80 rule</strong>: when you drop a <strong>high-power</strong> motor into the <strong>sustainer</strong> of a multi-stage rocket, the app defaults its ignition to <em>burnout + 1 s</em> (electronics-timed), because nobody lights a high-power sustainer directly off the booster's ejection charge. You can always override it.</p>\n<h2>Booster recovery and clusters</h2>\n<p>Boosters get their own recovery gear and their own landing-rate verdict in the report &mdash; a chuteless high-power booster is called out. <strong>Clusters</strong> (see <em>Designing the Rocket &rarr; Motor mounts and clusters</em>) let one motor selection drive several tubes; thrust scales with the count and mass sits at the real pattern positions.</p>"
  },
  {
    "id": "files-and-formats",
    "title": "Files, Units, and Offline Use",
    "html": "<p>Finally, save your work, move it between tools, and set up the app for offline use.</p>\n<h2>File formats</h2>\n<p>The app reads and writes several formats. What survives a round-trip depends on how much each format can represent:</p>\n<div>\n<table>\n<thead><tr><th>Format</th><th>Import</th><th>Export</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td><strong>.ork</strong> (OpenRocket)</td><td>Yes</td><td>Yes</td><td>Full fidelity &mdash; all 17 types, motors, materials, overrides, tabs, clusters, shoulders, staging. Accepts zipped or bare XML; handles legacy &le;15.03 files.</td></tr>\n<tr><td><strong>.rkt</strong> (RockSim)</td><td>Yes</td><td>Yes</td><td>Up to <strong>3 stages</strong> (export throws beyond that). Uniquely keeps motor designations the desktop drops, so it auto-loads motors. Clusters split into individual tubes; spill holes unsupported.</td></tr>\n<tr><td><strong>.CDX1</strong> (RASAero II)</td><td>Yes</td><td>Yes</td><td>Aerodynamics only &mdash; <strong>no mass or material data</strong>. Walls default to a faked 2 mm and the importer warns you to \"review masses before trusting the numbers.\" No motor mounts; engine names surface as a note. Strict export validation (&le;3 stages, one fin set per tube, 3&ndash;8 fins, conical transitions only).</td></tr>\n<tr><td><strong>.obj</strong> (Wavefront)</td><td>&mdash;</td><td>Yes</td><td>External shell only &mdash; the meshes the 3D view renders. Meters, nose at x=0. Not guaranteed watertight; for print-preview/CAD reference.</td></tr>\n<tr><td><strong>.svg</strong> (fin template)</td><td>&mdash;</td><td>Yes</td><td>True-scale 1:1 cut template with calibration ruler (see <em>Visualizing the Design</em>).</td></tr>\n</tbody>\n</table>\n</div>\n<p>The most important domain caveat is <strong>RASAero has no mass data</strong> &mdash; a <code>.CDX1</code> import gives you accurate geometry but placeholder masses, so treat its predicted altitudes as provisional until you add real materials and overrides. <code>.ork</code> is the only lossless format; use it as your working save.</p>\n<h2>Units and preferences</h2>\n<p>Open <strong>Preferences</strong> to switch between one-click <strong>Metric</strong> and <strong>Imperial</strong> presets, or set each quantity individually &mdash; length, motor dimensions, distance, mass, velocity, wind speed, acceleration, angle, density, temperature, and pressure. You also choose whether round parts are entered as <strong>diameter or radius</strong>. The same dialog sets the app's <strong>Theme</strong> &mdash; <strong>Light</strong>, <strong>Dark</strong>, or <strong>Follow system</strong> (which tracks your operating system's light/dark setting); this is what drives the theme-aware plot axes and the rest of the interface. Because everything is stored in SI internally, <strong>switching units never changes your design</strong> &mdash; it only changes how the numbers are displayed and entered.</p>\n<h2>Installing, offline, and saving your work</h2>\n<p>Online OpenRocket is a <strong>PWA</strong> &mdash; install it from the browser and it runs offline, since the physics kernel, the motor database metadata, and the preset catalog are all bundled locally. Only motor thrust curves fetch on demand, and once fetched they cache in your browser. Your work &mdash; the design tree, assigned motors, launch conditions, per-stage motor-length limits, run history, and motor filters &mdash; persists to local storage and survives reloads. The <strong>site menu</strong> carries the <strong>Guide</strong>, <strong>Changelog</strong>, and <strong>Preferences</strong>, alongside the New / Open / Save file actions. For a durable archive or to move a design to another machine or to the OpenRocket desktop, <strong>Save as .ork</strong> &mdash; it's the format that keeps everything.</p>\n<p>Because the whole app is a self-contained static build, the same files can also be <strong>embedded inside another web page</strong> &mdash; for example a WordPress post &mdash; through an <code>&lt;iframe&gt;</code>. If you meet Online OpenRocket living inside someone else's site rather than at its own address, it is the identical app running the identical kernel, with the same design, motor, and simulation tools described in this guide.</p>"
  },
  {
    "id": "how-it-works-physics",
    "title": "How It Works: Physics & Math",
    "html": "<p>Online OpenRocket is not an approximation <em>of</em> OpenRocket &mdash; it <strong>is</strong> OpenRocket. The simulation engine is the real OpenRocket 24.12 physics kernel (the Java <code>info.openrocket.core</code> package), compiled to JavaScript with TeaVM and verified bit-for-bit against the desktop program by an automated differential test. Every number below comes from that kernel: when your browser reports an apogee, it ran the same code, in the same order, with the same rounding as the desktop app. This section explains what that code does so you can decide how far to trust it &mdash; and where its honest limits are.</p>\n<p>Throughout, the engine works in <strong>pure SI units</strong> (metres, kilograms, seconds, newtons) and <strong>radians</strong>. Degrees, feet and grams exist only at the file-format and screen boundaries.</p>\n<h2>How a flight is simulated</h2>\n<p>The rocket is treated as a <strong>six-degree-of-freedom (6DOF) rigid body</strong>: three of position and three of orientation. Orientation is stored as a <strong>quaternion</strong>, never as Euler angles, which avoids gimbal-lock singularities when the rocket pitches over at apogee.</p>\n<p>The equations of motion are integrated with the classical <strong>fourth-order Runge&ndash;Kutta method (RK4)</strong>. Each step evaluates the derivatives (velocity, acceleration, angular velocity, angular acceleration) four times &mdash; at the start, twice at the midpoint, and at the end &mdash; and combines them:</p>\n<pre><code>y(n+1) = y(n) + (h/6)&middot;(k1 + 2&middot;k2 + 2&middot;k3 + k4)</code></pre>\n<p>RK4 is far more accurate for a given step size than simple Euler integration, and the fourth-order error term keeps energy drift small over a full flight.</p>\n<p><strong>Adaptive time step.</strong> The step length <code>h</code> is not fixed. Each step, the kernel takes the <em>smallest</em> of eight candidate limits, so the step shrinks automatically wherever the flight gets delicate:</p>\n<ul>\n<li>the user's requested time step (default 0.05 s), or one-fifth of it while still on the launch rod;</li>\n<li>a maximum pitch-angle change per step (default 3&deg;) and a maximum roll-angle step;</li>\n<li>limits on how fast the roll rate and the pitch/yaw rates may change in one step;</li>\n<li>one-tenth of the launch-rod length while the rocket is still on the rod;</li>\n<li>no more than 1.5&times; the previous step (smooth growth).</li>\n</ul>\n<p>A floor of 1/20th of the nominal step prevents numerical stall. This is why a wobbly, near-unstable flight is computed with many small steps while a clean boost coasts in large ones.</p>\n<p><strong>Phases of flight.</strong> Before lift-off the rocket cannot sink into the ground (downward acceleration is zeroed). While on the <strong>launch rod/rail</strong>, motion is projected onto the rod direction and angular acceleration is forced to zero &mdash; the rod holds the rocket straight until it clears. Once clear, full 6DOF applies: aerodynamic moments are shifted from the center of pressure to the current center of gravity, and pitch, yaw and roll accelerations follow from the body's moments of inertia.</p>\n<p>After a <strong>recovery device deploys</strong>, the rocket is no longer a rigid airframe flying nose-first, so the simulation switches from 6DOF to a simpler <strong>3-degree-of-freedom (point-mass) descent</strong>: it tracks position under gravity and parachute drag but not tumbling orientation. A parachute's drag coefficient defaults to <strong>0.8</strong>. An airframe that goes unstable without a chute is handed to a separate tumble model instead.</p>\n<h2>Aerodynamics &mdash; the Extended Barrowman method</h2>\n<p>Stability and normal force come from the <strong>Extended Barrowman method</strong> &mdash; the 1966 Barrowman equations, extended for body lift and for transonic/supersonic fins. The rocket's total normal-force coefficient slope (CN&alpha;) and center of pressure (CP) are built up component by component and summed.</p>\n<p><strong>Nose cones and transitions.</strong> A body of revolution generates normal force only where its cross-sectional area <em>changes</em>. For a transition from fore area <code>A&#8320;</code> to aft area <code>A&#8321;</code>, the kernel uses</p>\n<pre><code>CN&alpha; = 2&middot;(A&#8321; &minus; A&#8320;) / A_ref      CP = (L&middot;A&#8321; &minus; V) / (A&#8321; &minus; A&#8320;)</code></pre>\n<p>where <code>L</code> is length, <code>V</code> the enclosed volume and <code>A_ref</code> the reference area. A pure cylindrical body tube produces no area-change normal force at all.</p>\n<p><strong>Body lift (Galejs extension).</strong> Straight Barrowman ignores the lift a long body generates at angle of attack. The kernel adds the Galejs correction, with a coefficient of 1.1, proportional to the body's planform area and to <code>sin&sup2;(&alpha;)/&alpha;</code>, acting at the planform centroid. This is what keeps a long, small-finned rocket from looking falsely over-stable at high angle of attack.</p>\n<p><strong>Fins.</strong> A fin set's lift-curve slope in the subsonic regime is</p>\n<pre><code>CN&alpha;1 = 2&pi;&middot;s&sup2; / ( 1 + &radic;(1 + (1 &minus; M&sup2;)&middot;(s&sup2;/(A_fin&middot;cos&Gamma;))&sup2;) ) / A_ref</code></pre>\n<p>where <code>s</code> is the fin span, <code>A_fin</code> the fin area, <code>&Gamma;</code> the mid-chord sweep angle and <code>M</code> the Mach number. This is the standard finite-span lifting result: aspect ratio and sweep both reduce slope, and the <code>(1 &minus; M&sup2;)</code> term is the Prandtl&ndash;Glauert compressibility factor. Above Mach ~1.5 the kernel switches to a supersonic formula (with Mach-dependent K1, K2, K3 terms) and interpolates smoothly through the transonic gap. Multiple fins interfere with each other and with the body: fixed empirical factors reduce the per-fin slope for 4, 5, 6&hellip; fins, and a body-interference multiplier <code>(1 + &tau;)</code> accounts for the airflow the body diverts onto the fins.</p>\n<p><strong>Roll.</strong> Canted fins produce a roll-driving moment proportional to the cant angle, opposed by a <strong>roll-damping moment</strong> that grows with roll rate &mdash; so a canted rocket spins up toward a steady-state roll rate rather than accelerating forever.</p>\n<p><strong>Drag build-up.</strong> Total drag is assembled from four independent pieces:</p>\n<pre><code>CD = CD_friction + CD_pressure + CD_base + CD_override</code></pre>\n<ul>\n<li><strong>Skin friction</strong> comes from the Reynolds number and a skin-friction coefficient (laminar, turbulent or transitional), with corrections for surface roughness/finish, for compressibility near Mach 1, and a whole-rocket fineness-ratio correction <code>(1 + 1/(2&middot;f_B))</code>.</li>\n<li><strong>Pressure drag</strong> for nose cones is interpolated from wind-tunnel/free-flight data (the kernel embeds experimental curves for ogive, conical, ellipsoid, power, parabolic and Haack shapes from NASA TR-R-100), plus stagnation drag at any forward-facing area increase.</li>\n<li><strong>Base drag</strong> on the blunt tail follows <code>CD_base = 0.12 + 0.13&middot;M&sup2;</code> subsonic and <code>0.25/M</code> supersonic.</li>\n<li><strong>Override</strong> lets you pin a component's CD to a measured value.</li>\n</ul>\n<p>The drag coefficient is finally resolved into an axial component using an angle-of-attack multiplier, and pitch/yaw <strong>damping moments</strong> (which resist the rocket \"weather-vaning\" too sharply, especially the apogee turnover) are subtracted from the aerodynamic moments.</p>\n<h2>Mass, center of gravity and inertia</h2>\n<p>At every step the kernel computes the rocket's total <strong>mass</strong>, <strong>center of gravity</strong> and <strong>moments of inertia</strong> (longitudinal and rotational) by summing every component &mdash; walls, nose, fins, mounts, mass objects &mdash; from its geometry and material density, honoring any user overrides. The <strong>motor</strong> is then added as a separate rigid body: as propellant burns, both its mass and its own CG shift, read from the thrust curve's mass/CG data points, and the two bodies are combined. This is why your stability margin (the CP-to-CG gap, measured in calibers) changes continuously through the boost as propellant leaves the tail.</p>\n<h2>Atmosphere &mdash; the International Standard Atmosphere</h2>\n<p>Air properties follow the <strong>International Standard Atmosphere (ISA)</strong>, with sea-level defaults of <strong>288.15 K (15 &deg;C)</strong> and <strong>101 325 Pa</strong>, and a troposphere temperature <strong>lapse rate of &minus;6.5 K per km</strong> up to 11 km. Above that the kernel models the full layered ISA profile: the isothermal tropopause to 20 km, the warming stratosphere, and so on up past 84 km.</p>\n<p>Within each layer, pressure follows the <strong>barometric formula</strong>. For a layer with a non-zero lapse rate <code>L</code>:</p>\n<pre><code>p = p_b &middot; (1 + (h &minus; h_b)&middot;L / T_b)^(&minus;g / (L&middot;R))</code></pre>\n<p>and the isothermal exponential form where the lapse rate is zero. Temperature is linear within a layer; density and speed of sound then come from the ideal-gas relations <code>&rho; = p/(R&middot;T)</code> and <code>a = &radic;(&gamma;&middot;R&middot;T)</code> with <code>&gamma; = 1.4</code>. You can override the launch-site temperature and pressure, and the model rebuilds a consistent profile above you. One documented caveat carried straight from OpenRocket: above ~32 km the layered values drift from the exact standard by a few percent &mdash; irrelevant for essentially all hobby altitudes.</p>\n<h2>Gravity and geodesy</h2>\n<p>Gravity uses a <strong>WGS84 ellipsoid</strong> model rather than a single constant. Sea-level gravity varies with latitude &phi; via the Somigliana formula:</p>\n<pre><code>g&#8320; = 9.7803267714 &middot; (1 + 0.00193185138639&middot;sin&sup2;&phi;) / &radic;(1 &minus; 0.00669437999013&middot;sin&sup2;&phi;)</code></pre>\n<p>so it is weaker at the equator and stronger at the poles. An altitude correction <code>(R_earth / (R_earth + h))&sup2;</code> reduces it as the rocket climbs. When a launch latitude/longitude is supplied and geodetic computation is enabled, the kernel also adds the <strong>Coriolis acceleration</strong> from Earth's rotation. The altitude correction assumes a spherical Earth &mdash; the kernel's own comment notes this is a deliberately small approximation.</p>\n<h2>Motor thrust-curve model</h2>\n<p>A motor is a table of measured samples: time, thrust, remaining mass and propellant CG, exactly as published in RASP/RSE thrust-curve files. At any simulation instant the kernel finds the bracketing samples and does <strong>linear interpolation</strong> between them (snapping to a sample when it lands within 0.1 ms of one). Instantaneous thrust, motor mass and motor CG are all read this way; total impulse and average thrust are derived from the same curve. There is no internal combustion model &mdash; the certified thrust curve <em>is</em> the input, which is exactly what you want for matching real motor performance.</p>\n<h2>Wind, turbulence, and why our results are repeatable</h2>\n<p>Wind is modeled as <strong>pink noise</strong> around a mean speed. You set an average wind speed and a standard deviation (their ratio is the <em>turbulence intensity</em>); the kernel drives a two-pole pink-noise filter (spectral exponent &alpha; = 5/3, sampled every 0.05 s and linearly interpolated) to produce gusts with a realistic frequency content &mdash; more low-frequency wander than white noise. In this single-level model the wind blows in one horizontal direction and does not vary with altitude.</p>\n<p>There is also a deliberately tiny random perturbation (&plusmn;0.0005) added to the pitch and yaw moments each step, which prevents an unnaturally perfect, knife-edge-symmetric flight from never tipping over.</p>\n<p>Here is the one intentional difference from the desktop. Desktop OpenRocket seeds these random streams from the wall clock, so two identical runs give slightly different gust histories. <strong>Online OpenRocket seeds them deterministically</strong> (a fixed default seed, 42, unless you choose another). Same design + same settings + same seed &rArr; <strong>exactly the same flight, every time</strong> &mdash; better for teaching, sharing and comparing. Change the seed to sample a different gust realization; hold it fixed to make a clean before/after comparison of a design change. (For the same reason the engine also fixes a couple of internal iteration orders that the desktop leaves free to wander at the sub-rounding-error level. Physics results stay inside the desktop's own run-to-run envelope.)</p>"
  },
  {
    "id": "limitations-and-references",
    "title": "Assumptions, Limitations & References",
    "html": "<p>The physics above is powerful, but every simulation makes modeling choices. Knowing them tells you exactly where to trust the numbers and where to add engineering margin.</p>\n<h2>Assumptions and honest limitations</h2>\n<ul>\n<li><strong>Speed regime.</strong> Extended Barrowman is at its best <strong>subsonic and through the transonic region</strong>. Fins get a genuine supersonic treatment, but body CN&alpha; and CP above Mach 1 are assumed equal to their subsonic values, and the engine raises a <em>supersonic</em> warning past Mach 1.1. Treat high-supersonic stability numbers as indicative, not precise.</li>\n<li><strong>Small-angle / attached-flow aerodynamics.</strong> The method assumes slender bodies and largely attached flow. A stall angle around 17.5&deg; caps fin normal force, and the body-lift term is damped at very low speed near apogee to avoid an artifact. Extreme angles of attack are approximations.</li>\n<li><strong>Descent is a point mass.</strong> Under parachute the rocket is 3DOF: drag (default Cd 0.8) and gravity only. Swinging, spilling, and canopy dynamics are not modeled, and <strong>streamers</strong> &mdash; especially at extreme length-to-width ratios &mdash; are represented by an effective drag area, not by true flexible-body aerodynamics.</li>\n<li><strong>Wind is horizontal and altitude-uniform</strong> in the standard model; real wind shear and vertical gusts are not captured here.</li>\n<li><strong>Geodesy approximations.</strong> The altitude gravity correction assumes a spherical Earth; the atmosphere drifts a few percent above ~32 km.</li>\n<li><strong>The thrust curve is trusted verbatim.</strong> Motor-to-motor manufacturing variation, temperature effects on propellant, and off-axis thrust are outside the model.</li>\n</ul>\n<p>None of these are bugs &mdash; they are the modeling choices of a mature, widely validated tool. Because you are running that tool's actual kernel, its accuracy <em>and</em> its limits are inherited exactly, and your results will match the OpenRocket desktop program.</p>\n<h2>Licensing and source code</h2>\n<p>Online OpenRocket is <strong>free software</strong>, released under the <strong>GNU General Public License, version 3 or later (GPL v3+)</strong> &mdash; the same license as OpenRocket itself, which it inherits. You are free to use, study, share, and modify it under that license. Because the GPL requires that anyone running a distributed build be offered its corresponding source code, the app's header carries a <strong>source (GPL)</strong> link &mdash; titled <em>\"This app is free software under the GPL v3 or later &mdash; source code for this build\"</em> &mdash; that opens the public source repository. If you want to see exactly what the app does, that link is the front door.</p>\n<h2>References &amp; further reading</h2>\n<ul>\n<li><strong>Barrowman, J. S., &amp; Barrowman, J. A. (1966).</strong> <em>The Practical Calculation of the Aerodynamic Characteristics of Slender Finned Vehicles.</em> (J. S. Barrowman, M.S. thesis, The Catholic University of America, 1967.) The origin of the center-of-pressure and normal-force method used here.</li>\n<li><strong>Niskanen, S. (2009, rev. 2013).</strong> <em>Development of an Open Source model rocket simulation software</em> / <em>OpenRocket technical documentation.</em> The definitive derivation of OpenRocket's extended Barrowman aerodynamics, drag build-up, mass model and simulation loop. <a href=\"https://openrocket.info/documentation.html\">https://openrocket.info/documentation.html</a></li>\n<li><strong>Galejs, R. (2006).</strong> <em>Wind instability &mdash; what Barrowman left out.</em> Sport Rocketry / online. Source of the body-lift extension applied to long airframes.</li>\n<li><strong>NASA (1963).</strong> <em>Collection of Zero-Lift Drag Data on Bodies of Revolution from Free-Flight Investigations,</em> NASA TR-R-100 (NTRS 19630004995). Source of the nose-cone pressure-drag data tables.</li>\n<li><strong>International Standard Atmosphere:</strong> ISO 2533:1975; and the <em>U.S. Standard Atmosphere, 1976</em> (NOAA/NASA/USAF). Basis of the temperature, pressure and density model.</li>\n<li><strong>WGS84 / Somigliana gravity formula:</strong> <em>Department of Defense World Geodetic System 1984,</em> NIMA TR8350.2. Basis of the latitude- and altitude-dependent gravity model.</li>\n<li><strong>Runge&ndash;Kutta integration:</strong> Butcher, J. C., <em>Numerical Methods for Ordinary Differential Equations;</em> or Press et al., <em>Numerical Recipes,</em> ch. 16. The RK4 scheme used for the equations of motion.</li>\n<li><strong>OpenRocket project:</strong> <a href=\"https://openrocket.info\">https://openrocket.info</a> &mdash; source at <a href=\"https://github.com/openrocket/openrocket\">https://github.com/openrocket/openrocket</a>. Licensed under the <strong>GNU General Public License v3.0-or-later</strong>: <a href=\"https://www.gnu.org/licenses/gpl-3.0.html\">https://www.gnu.org/licenses/gpl-3.0.html</a> . Online OpenRocket inherits this license.</li>\n</ul>"
  }
];
