# Session handoff — 21 August 2026, fifth sitting (BATCH 08-21d BUILT: v0.050 live — Stage B, 3D see-through, forum post UNGATED)

Self-contained. Read this before `working-notes.md`'s per-version history. Supersedes
`docs/handoff-2026-08-21d.md`.

## ⚡ ADDENDUM (same sitting): v0.051 shipped on Eric's chat report

Eric (chat + screenshot, moved to the Dropbox reference folder as
`eric-chip-zoom-report-2026-08-21.png`): zoomed in on the rocket, the fixed stats
chip was "a giant black panel" over the drawing. **v0.051**: the chip is genuinely
floating — drag anywhere on the canvas (clamped), fold to a one-line stability pill
(▾ button; a still click on the pill unfolds), position + fold persisted under
`online-openrocket.chip.v1`, opacity eased to 0.78. StatsChip owns the behavior
(StatTiles.tsx) + StatsChip.test.tsx (4 tests → 26 engine + 632 app green).
Drag/fold/reload-persistence verified live in-browser; deployed + site synced to
0.051.

## One-line state

**v0.050 is live and verified at https://mmrsim.mountainmanrockets.com** — multi-config
**Stage B** (flight configurations as switchable presets), the three 3D notes fixed
(slim axis balls, Reset/Side/Aft recovery, genuinely see-through shell showing the
motor), the S1 vertical+drawer collision fixed, and **both invite copy calls closed
per Eric — nothing gates the forum post anymore except Eric posting it**. Response:
`docs/testing/response-2026-08-21d.md`. Site tools page synced (0.050, deployed).

## ⚠️ FIRST on the DESKTOP (E:\git\online_open_rocket)

1. `git remote set-url origin https://github.com/mtnmanak/mmrocket-sim.git` (if still
   pending). 2. `git fetch` + confirm local == origin/main.

## What shipped (commits `76d1c66`-ish quick fixes + `db6a6f8` Stage B; all deployed)

1. **Stage B (working-set + presets model):** `mountMotors` stays the live set every
   consumer reads; `savedConfigs: SavedConfig[]` (`{id, name, isDefault, motors:
   Record<mountId, MountMotor>, unmatched?}`) + `activeConfigId: string | null` ride
   App state and session autosave. importOrk's `configs[]` now carry per-config
   resolved motors+ignition; App matches ALL configs against the motor DB in one pass
   (`matchImportedMotor` factored out). `ConfigPanel.tsx` renders on Motors & Launch
   when `savedConfigs.length > 1` (deliberate — single-config files would show noise;
   ⏏ Unload covers their "None"). Export writes every config (stable ids, `<name>`
   when non-null, default="true" on active — active's motors from the LIVE set; an
   active-null custom set exports as one extra unnamed default config). Picker modal
   gains "Open with no motors loaded". `SimRun.flightConfig` + trailing "Flight
   config" CSV column (`motorConfig` = cluster combos, untouched). Import note now
   credits the panel (test-pinned wording updated).
2. **Eric's "why open with a motor loaded?"** — answered, not just implemented:
   default-loaded kept (desktop parity, the field open→Launch flow, share links), his
   instinct honored via the no-motors picker option + None row + Unload. Flipping the
   default is one line if he converts after using it.
3. **3D (his three notes):** axis balls 0.45× the size rule (gadget unchanged);
   Reset/Side/Aft buttons (`jumpTo` — OrbitControls re-derives from camera; set
   position+target+update) + min/maxDistance zoom limits; REAL see-through — shell
   opacity 0.55 + **glassy inner tubes** 0.5 (`Piece.innerGlass`), both depthWrite
   OFF with renderOrder layering (opaque motor → glass mount → shell) and DoubleSide.
   The v0.049 pass (0.88, depth writes on) *looked* opaque, and the opaque mount hid
   the motor entirely — that was the actual bug behind "we aren't comparable yet".
4. **S1 fix:** the hero view lifts above the open stats drawer (measured via
   ResizeObserver on the drawer, `bottom: clearance`) — vertical mode is read-only,
   so there was no escaping the old overlap.
5. **Invite:** migration note dropped, no non-affiliation clause (Eric: "move on from
   the old subdomain"), version line rolled — `docs/beta-invite.md` is copy-paste
   ready. **The forum post is Eric's manual step and nothing else gates it.**
6. Guide: flight-configurations section + 3D buttons wording; changelog v0.050.

## Verified

26 engine + 628 app tests (15 new: Stage B export/round-trip, per-config presets,
session, ConfigPanel, CSV column), tsc + build clean, gate consistent. **E2E in a real
browser against a real two-config .ork** (built from the test fixture): picker with
default marked + no-motors option; opening the NON-default config (re-parse path);
panel showing per-mount truth (Demo day correctly missing the booster motor); Apply
switching (vitals followed); None clearing; stage-activeness note. Plus vertical+
drawer fix and 3D layering confirmed by screenshot. Live: version.json 0.050, deploy
workflow green, site check-tools "all in sync" + deployed.

## ⏸️ RESUME POINT — waiting on Eric

`response-2026-08-21d.md`: (1) **post the forum invite** — ungated; (2) look at
v0.050 (3D internals — next lever if still short of RocketForge is a per-part
contrast/color pass; the configurations panel with his own multi-config files);
(3) standing queue: Stage C + geodetic (next engine rebuild), supersonic default-ON
(ARCAS anchor), design optimization (post-beta), S4 stage 2 + S5 polish (beta
feedback).

## Known nits / follow-ups (no Eric input needed)

- Vertical margin text can brush the CG label on degenerate designs whose callout
  lanes nearly touch (the counter-rotated text's length isn't part of the lane
  geometry). Cosmetic; queued.
- bp_calculator + motor_dashboard still trail chrome.ref 1.3.0 (their repos).
- Optional tidy-up: hostMigration/MovedNotice removal (dead since the 301).
- A local `vite preview` on :4173 may linger from visual checks — dies with the
  terminal. The two-config test .ork lives in the session scratchpad only.
