# Session handoff — 21 August 2026, session close (v0.050 → v0.052 live; FORUM POST IS THE NEXT MOVE)

Self-contained; supersedes `docs/handoff-2026-08-21e.md` (which holds the fifth
sitting's full detail — this consolidates the close). Read this before
`working-notes.md`'s per-version history.

## One-line state

**v0.052 is live and verified at https://mmrsim.mountainmanrockets.com.** Every item
from batches 08-21c and 08-21d plus Eric's two chat reports is shipped, tested, and
deployed; the site tools page is synced (0.052); both repos' trees are clean and
pushed. **Nothing gates the public forum beta post — posting `docs/beta-invite.md` is
Eric's next move.**

## What this session shipped (three releases, all live-verified)

- **v0.050** — multi-config **Stage B**: flight configurations as switchable presets
  (ConfigPanel on Motors & Launch when a file carries >1; working-set + presets
  model; export writes ALL configs back with stable ids, default on active; "Open
  with no motors loaded" in the picker; sessions/CSV carry configs). 3D: axis balls
  0.45×, Reset/Side/Aft + zoom limits, REAL see-through (shell 0.55 + glassy inner
  tubes, depthWrite off — the opaque mount had hidden the motor entirely). S1
  vertical+drawer collision fixed. Invite cleared per Eric's copy calls.
- **v0.051** — the stats chip is genuinely floating: drag anywhere (clamped), fold to
  a stability pill, position+fold persisted (`online-openrocket.chip.v1`).
- **v0.052** — the hero drawing surface spans the FULL canvas (`fillHeight` on
  TreeSchematic): the "large black box the rocket slides under" was the svg's
  invisible clip edge from the adaptive-height, centered drawing; ~90px dead strips
  are gone, controls sit at the true canvas corner. Verified at ×6 zoom.

Earlier same day (separate handoffs, all superseded): v0.047 rename shipped, phase-2
301 fired, v0.048 (tour/callouts/mass-type picker), v0.049 (hero canvas, 3D gadget,
S5, Fly screen, Stage A).

## Verified at close

26 engine + 633 app tests green; tsc + build clean; version gate "0.052 is
consistent"; deploy workflow green; live version.json 0.052; site `check-tools.mjs`
"all tools reachable and in sync" and deployed. Chip drag/fold/persistence,
full-canvas zoom, Stage B end-to-end (real two-config .ork: picker, non-default
open, panel switching, None), 3D layering — all verified in a real browser.

## ⚠️ FIRST on the DESKTOP (E:\git\online_open_rocket)

1. `git remote set-url origin https://github.com/mtnmanak/mmrocket-sim.git` (if still
   pending from the repo rename). 2. `git fetch` + confirm local == origin/main.

## ⏸️ RESUME POINT — waiting on Eric

1. **Post the forum beta invite** (`docs/beta-invite.md`, copy-paste ready — his
   manual step; ~week of 08-24 was the plan).
2. Eyeball v0.050–52 live: the 3D internals (next lever if still short of
   RocketForge: a per-part contrast/color pass — cheap), the configurations panel
   with his own multi-config files, the now-floating chip + full-canvas zoom.
3. Standing queue: multi-config **Stage C** + geodetic selector (both ride the next
   engine rebuild), supersonic default-ON (still blocked on the ARCAS anchor),
   design optimization (post-beta), S4 stage 2 bottom sheets + S5 polish (beta
   feedback). Eric may also revisit "open with no motors as the DEFAULT" after
   living with v0.050 — a one-line flip, reasoning recorded in
   `response-2026-08-21d.md`.

## Known nits / follow-ups (no Eric input needed)

- Vertical margin text can brush the CG label on degenerate close-lane designs
  (cosmetic; the counter-rotated text's length isn't in the lane geometry).
- bp_calculator + motor_dashboard still trail chrome.ref 1.3.0 (their own repos;
  site check-tools flags bp_calculator).
- Optional tidy-up someday: remove `hostMigration.ts`/`MovedNotice` (dead since the
  301) — and consider drag-and-remember for the 3D CG/CP gadget (offered to Eric).
- Eric's report screenshots live in the Dropbox reference folder
  (`eric-chip-zoom-report-2026-08-21.png`, `RocketForge Screenshots/`).

## Environment

No background jobs, workflows, or worktrees left running (a local `vite preview` on
:4173 from visual checks dies with the terminal). The S1/S5/S4 mockup design canvas
from this session: https://claude.ai/code/artifact/1b9fe671-771e-4d48-8bcf-f537315f63f1
(its editable sources were session-scratchpad-only — a future session edits it via
the artifact itself).
