# Session Handoff — 2026-08-05 (Online OpenRocket)

**Read this first when starting a fresh session.** Supersedes
`docs/handoff-2026-08-04.md` (still valid for the RASAero-supersonic-build
history and its gotchas — most of §5 there still applies). Deeper context:
`docs/working-notes.md` (per-version detail), `CLAUDE.md`,
`engine-java/patches/LEDGER.md`, `validation/README.md`.

---

## 1. Current state (TL;DR)

- **Version: v0.040, LIVE** at https://www.mountainmanrockets.com/online_open_rocket/
  — verified 2026-08-05 cache-bypass: served bundle `index-8ZgGK_NG.js`
  matches the final local build at full byte size, version.json serving
  0.040, v0.040 badge; Eric's live session already shows the batch-e
  filename-fallback fix working (his re-imported Ultra Neon carries the
  filename as its name).
- **Tests: 196 green** (app 173 in 20 files, engine 23 in 2 files).
  **Differential: 258 golden lines** — NO engine rebuild in the v0.040
  session (app-side only), so the ×3-verified state from the fin-rotation
  rebuild still stands. Git clean on `main`, everything pushed.
  Next version **v0.041**.
- **Eric is BETA TESTING**, one dated issues file (or chat batch) at a time,
  fixed same-day. Standing rule unchanged: his next issues file in
  `docs/testing/` gets fixed before any new feature work.
- 2026-08-05 shipped **EIGHT releases (v0.033–v0.040)** across six issue
  batches (a–e + two chat batches): the full 2026-08-05a 19-item batch, the
  camera-shroud component, spill holes, customizable result tiles, the
  unified aero-model picker (Rogers Kbf now the DEFAULT), XLSX export,
  combination batching (2+2 / 3+3 / 4+2 / 2+2+2), batch mount picker,
  mount-level max motor length, fin rotation + auto-interleave, tolerant
  RockSim cluster reconstruction, ExternalPod↔pods, one live-site hotfix
  (v0.035 aft-pan crash), the aft end view — and batch e (v0.040): fin
  auto-align button, camera-shroud convert-on-import, tube-fin collision
  caps + auto placeholder, sub-minimum rockets, EX motor folder import.

### Resume commands (unchanged)
```
cd G:\git\online_open_rocket
npm test --workspaces            # engine + app = 196 tests
npm run package                  # build + deploy/online-openrocket-v<ver>.zip
node validation/score.mjs --supersonic
```
Engine rebuild ritual: `gradlew.bat generateJavaScript` in engine-java/
(JAVA_HOME = portable JDK at C:\Users\Eric\.online-openrocket\jdk-17.0.19+10)
→ `node engine-java/scripts/build-engine.mjs` → difftest ×3 **AND** engine
vitest. Not needed for v0.040 (app-side only).

### v0.040 (issue batch e) in one paragraph

`issues-2026-08-05e.md` → `response-2026-08-05e.md` (READ IT — it carries
the open feedback questions). New modules: `tree/finAlign.ts` (🧭 auto-align
button: max-min clearance grid search over overlapping sibling fin sets —
also covers the case v0.039's add-second-set default can't: a 6-fin set
offset by 60° is a no-op against its own pitch), `tree/shroudConvert.ts`
(every import detects 1-fin freeform sets named /shroud|camera|fairing/i and
a modal offers one-click conversion to the native fairing — same node id,
dims from the outline, mass from override else area×thickness×density).
Tube fins: collision caps on EXPLICIT values (OD ≤ touching radius, count ≤
fit; PropertyPanel cross-limits) + blank OD shows `auto: <value>` grayed +
fixed the n=2 auto-radius ÷0 in tubefins.ts. Sub-minimum: `caseAirframe`
bool on body-tube mounts → motor browser fits by OUTER diameter (the fit
filter was the only blocker; the sim never gated); AftView now draws motors
in body-tube mounts; `<caseairframe>` .ork extension tag. EX motors existed
since 2026-07-03 (commit e2c298a) but were undiscoverable — picker button
renamed, multi-file + 📁 folder import (webkitdirectory, one-shot scan),
import now clears maker AND diameter chips (a persisted 38 mm chip hid a
fresh 54 mm import), result notice, new guide section (both mirrors).
Drive-by: GENERIC_ROCKET_NAMES gained the importer fallbacks so name-less
.rkt/.CDX1 files take the filename.

---

## 2. ⚡ QUEUED FOR NEXT SESSION (Eric's explicit ask, 2026-08-05 close)

> **RESOLVED by issue batch e (v0.040, same day):** Eric's
> `issues-2026-08-05e.md` gave direction on both queued items — shroud
> import conversion (prompt + one-click convert, shipped) and fin-rotation
> follow-up (auto-align button, shipped). See
> `docs/testing/response-2026-08-05e.md` and the v0.040 working-notes block.

**What's actually open going into the next session:**

1. **Standing rule first**: if a new dated issues file is waiting in
   `docs/testing/`, fix it before anything else.
2. **Eric's feedback on the batch-e judgment calls** (listed in
   `response-2026-08-05e.md` §"Waiting on you"): shroud-conversion defaults
   (half-round shape + area×thickness×density mass estimate — right?);
   whether DECLINING a shroud conversion should be remembered per file
   (currently every reopen re-asks); whether sub-minimum's outer-diameter
   fit is enough for odd-size EX cases (the 1.5 mm class snap / +1 mm mount
   tolerance in motorDb.ts still applies).
3. **Fin-rotation leftovers deliberately not built** (Eric never asked; from
   the old queue): 2D side-view still a stylized projection ignoring
   rotation; no LIVE collision warning while editing (the 🧭 button is
   on-demand); per-fin angles vs whole-set rotation.
4. Still open from earlier batches (Eric aware, no pressure): RockSim full
   gap-audit sweep (optional, ~half session); camera-shroud calibration
   flight (first anchor for the Hoerner+strake model); live-fire of the
   Darkstar / Ultra Neon combination batches.

---

## 3. What shipped 2026-08-05 (v0.033 → v0.040, one day)

Full per-version detail in `docs/working-notes.md` and the response docs
(`docs/testing/response-2026-08-05{a,b,c,d,e}.md` — Eric wants the durable
issues/response file protocol continued). v0.040 is summarized in §1;
v0.033–v0.039 highlights by theme:

**Physics & modeling**
- **Rogers Modified Barrowman (Kbf) is now the DEFAULT** (Eric: matches his
  flight data). Explicit stored `rogersKbf:false` preserved; "off = exact
  desktop parity". Tagline is now his wording: *"Design, simulate, fly —
  OpenRocket-derived physics, validated to Mach 4.6 against NASA
  wind-tunnel data."* All user-facing "RASAero-class" labels scrubbed —
  it's OUR model (open-literature build; guide keeps RASAero as reference).
- **Camera shroud / `fairing` component** (v0.034): app-level type lowered
  at the engine boundary (`treeModel.engineTree`) to a 1-fin freeform
  strake (Barrowman low-AR = Jones slender-strake ⇒ real kernel CP shift)
  + Hoerner frontal-area protuberance CD as overrideCD (streamlined .25 /
  halfround .55 / box 1.05) + as-built overrideMass. Same node id
  preserved. NO kernel change. THICK_FIN warning suppressed for shrouds.
  No wind-tunnel anchor yet — flagged honestly everywhere.
- **Parachute spill holes**: cd·(1−(dh/D)²) at the engine boundary;
  RockSim SpillHoleDia round-trips; .ork extension tag.
- **Preset Cd bug (the big one)**: presetPatch never copied
  dragCoefficient — 230/459 chute presets flew at 0.8 instead of their
  rating (Iris Ultra 2.2 ⇒ 1.66× fast descent). Fixed; IFC-144-S now
  matches Fruity's calculator within ~2%.
- **Fin rotation** (v0.039): the session's one ENGINE REBUILD — bridge
  wires kernel setBaseRotation for FinSet types AND TubeFinSet. 'rotation'
  field on all four fin-set types; 3D + aft views draw true angles; .ork
  `<rotation>` (was hard-coded 0.0 on export!) + RockSim RadialAngle.
- **Stability tiering**: one rule everywhere — <1 cal red ⚠, 1–3 green ✓,
  >3 yellow △ caution (thresholds Eric-confirmed).

**Batching**
- Batch dialog now has: **aero-model select** (default Auto, per-motor
  supersonic refly), **mount picker** (any mount; others keep their
  loaded motors), and **combination modes** — all opt-in, default = same
  motor in every tube:
  - "mixed 2+2" (4-ring) / "mixed 3+3" (6-ring): cluster split in halves
    (`treeModel.splitClusterTree` — 4-ring→2×double ×√2 ±45°, 6-ring→
    2×3-ring ×√3 0/60°), unordered candidate pairs.
  - "mixed 4+2 / 2+2+2" (6-ring): `splitClusterPairsTree` → THREE 'double'
    mounts on the diagonals (×2, 90°/±30°), candidate MULTISETS minus
    all-same. Cubic growth — count line warns.
  - All splits geometry-tested exactly against clusterOffsets; kernel-level
    proof in `comboFlight.test.ts` (split-same ≡ original within 1%).
- **Batch flies its OWN engine handles** (design tree + split variant) —
  the shared design handle is never mutated (stale-motor class eliminated).
- Exports: SimRun.motorConfig + "Motor config" column; CSV sorted by
  config group; **XLSX one tab per config + All** (multi-sheet writer).

**Files & import fidelity**
- RockSim cluster reconstruction with **1% tolerance grouping** (RockSim
  rounds the same tube differently between copies — Darkstar 79.38 vs
  79.375; verified on his real file: 6×75 ring + central 98).
- **Auto-interleave**: overlapping same-angle fin sets rotate by half the
  other's pitch on import + note (Ultra Neon verified); editor
  add-second-set defaults likewise.
- ExternalPod ↔ podset/parallelstage both directions (desktop semantics;
  Detachable ↔ booster; N instances split). Fin CantAngle round-trips.
  RingTail stays ignored (desktop parity). Partial overrides export the
  COMPUTED other value (engine componentInfo threaded into exportRkt).
  RASAero export crash fixed (full 24-element Simulation block).
- **Max motor length lives ON the mount tube** (schema + .ork extension
  tag); Motors-tab field is a per-stage override (placeholder "design: X",
  clear = fall back). Root cause of "doesn't persist": session-only state
  wiped on .ork open.
- **XLSX export**: minimal OOXML writer over fflate (~2 KB — NO
  spreadsheet library in the PWA); typed cells, bold frozen header,
  autofilter, sized columns; buttons beside every CSV.

**UI**
- Aft end view (2D/3D/**Aft** toggle + auto inset on Motors tab for
  clusters/pods) with wheel-zoom/pan (hotfix v0.035: NEVER read a mutable
  ref inside a setState updater — pointerup nulls it first).
- Customizable result tiles (⚙ picker, 14 metrics, prefs.resultTiles;
  "Descent hits" renamed Landing rate — it was groundHitVelocity).
- One aero pulldown (4 explicit options; Kbf checkbox gone).
- Coupler/shoulder overhangs visible (2D overlay paint pass); inner
  components differentiated (glyphs: chute/mass/CR/cord; bulkhead hatch;
  colors + tags); two-way selection sync (click drawing ↔ tree,
  drag-safe); component cut/copy/paste with containment-checked paste
  targets; plugged-motor option everywhere (-P labels, Infinity persisted
  as "Infinity" in JSON); saved sims tagged to their rocket; recovery
  weight in the report; 3-decimal CP/CG/length readouts; CSV detail
  columns follow unit prefs (flight-day lead 14 stay fixed); header Undo;
  vitals ⏏ unload-all-motors; notification clears on New.

---

## 4. Nuances & gotchas (new this session — see 08-04 handoff for older)

- **JSON.stringify(Infinity) → null.** Plugged delays persist as the
  string "Infinity" via replacers in simStore AND session.ts. Any new
  persisted field that can be Infinity needs the same treatment.
- **RockSim rounds the same dimension differently between copies** —
  never exact-match-group imported geometry; use tolerances.
- **RockSim stores NO fin interleave angle** (RadialAngle 0 on everything)
  — its renderer fakes it. Import-side de-collision is ours.
- **setState updaters run after the handler returns** — capture mutable
  refs into locals first (v0.035 crash).
- **The shared engine handle is sacred**: anything that flies alternates
  (batch) must build its own handle from the tree. There is no
  clear-motor API and no per-handle dispose; handles free on the next
  resetEngine (App build memo).
- **fairing/spill-hole lowering lives in `treeModel.engineTree()`** — any
  new app-level physics concept goes there, keeping ids stable.
- **Eric's real .rkt files are gold for verification** — paths in
  `G:\Documents\Dropbox\Rocksim Designs\Scratch Builds\` (Darkstar, Ultra
  Neon). Verified via throwaway vitest files reading the absolute path
  (create → run → DELETE; never commit).
- **deg schema fields auto-convert to radians** via PropertyPanel's LEGACY
  map — new angle fields just use unit 'deg'.
- **Sheet names in xlsx**: ≤31 chars, no `: \ / ? * [ ]` — sheetsToXlsx
  sanitizes; keep it that way.
- The tolerant cluster grouping + de-collision + reconstruction all run in
  importRkt in ORDER: convert → reconstructClusters → deCollideFins.

New in the v0.040 session:

- **π/count rotation offsets are a no-op when the target set's own pitch
  divides them** — v0.039's add-second-set default and the import
  de-collision both have this blind spot (6-fin set offset by 60° = still
  colliding). The 🧭 button's max-min-clearance grid search is the robust
  form; consider porting it into the import path if Eric hits it there.
- **Persisted MotorBrowser filters can hide what you just imported** — any
  new "add to the list" flow must clear (or check against) the stored chip
  state in `online-openrocket.motor-filters.v1`.
- **Importer default names must be in GENERIC_ROCKET_NAMES** (App.tsx) or
  the filename fallback silently never fires for that format.
- **webkitdirectory** needs the `{...({ webkitdirectory: '' } as
  Record<string, string>)}` spread in TSX (not in React's typings); it's a
  one-shot scan — a PWA cannot watch a folder.
- Smoke-test hygiene that worked well: back up
  `online-openrocket.session.v1` (localStorage, not sessionStorage) to a
  temp key before mutating, restore + reload after, and filter test EX
  motors out of `online-openrocket.ex-motors.v1` by designation.

## 5. Session commits (all on main, pushed)

```
26c2193 docs: v0.040 live-verified (batch e deployed)
8531135 v0.040: issue batch 2026-08-05e — fin auto-align, shroud convert on import, sub-minimum, EX folders
67ce48a docs: session handoff 2026-08-05 (v0.033-v0.039 live-verified; next-session discussions queued)
fd23a42 docs: working notes updated for v0.039 (issue batch d shipped)
196f5c7 v0.039: fin rotation + auto-interleave + 4+2/2+2+2 cluster combos (batch d)
f1c924f docs: working notes updated for v0.038
089ec64 v0.038: Darkstar cluster import, batch mount picker, unload-motors button
7ecf90a docs: working notes updated for v0.037 (combination batching shipped)
7aaa737 v0.037: mixed-motor combination batching + max motor length on the mount
1c359eb docs: working notes updated for v0.036 (issue batch c shipped)
ef67860 v0.036: issue batch 2026-08-05c — one model picker, batch aero, XLSX, RockSim pods
994c46b docs: working notes for the v0.035 hotfix
14204e1 v0.035: hotfix — aft-view pan crash (stale ref in setZoom updater)
0ba1ee5 docs: working notes updated for v0.034 (issue batch b shipped)
f7396ba v0.034: issue batch 2026-08-05b — camera shrouds, spill holes, custom tiles
d55c839 docs: working notes updated for v0.033 (issue batch 2026-08-05a shipped)
3c9a93e v0.033: beta issue batch 2026-08-05a — 16 of 19 items fixed/built
```
