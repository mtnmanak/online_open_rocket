# OpenRocket Online — Notes & Feature Requests
**Date:** 21 August 2026 

# Part 1 — the RocketForge note

- **Screenshots housekeeping**: 
 Recommendation: move them to the Dropbox reference folder
- Move them

### S1. Make the rocket the hero of the screen (desktop)

- I like this one, but I would like to see it in action first - let's do the mockup so I can tell if I like it when implemented

### S2. CG/CP leader-line callouts on the drawing

- agreedm i like the way they do the flying cp/cg markers - let's try it

### S3. First-run tour (this also closes the "guide nudge" item from Part 2)

- yes, let's try the 6-step guide with auto-shut off after the first time the user uses the tool, with replay button and option in preferences to turn the feature off. we can always add or delete steps later once the framework is built and based on user feedback

### S4. Phone: vertical rocket, sheet-based panels (staged)

- i think your suggestions are correct, but let's take a moment to think about this a bit. let's think about working backwards from the user and what the likely workflow is. I think users will mostly be doing design and building on a desktop browser. I think the phone versions will mostly be used at the field to sim motors and recovery options they plan to use that day. so, with that in mind, how would be optimize the mobile experience for sim'ing rather than designing. Put another way, the desktop version should be design heavy, but the mobile version should be launch centered. 

### S5. Visual richness of parts and internals — selectively

- excllent suggestions, let's mock it up and let me see how it looks


---

# Part 2 — closing out the annotated survey

- clarification on the beta send out - I have shared it selectively with individuals, not as a full public send out, so it is correct to say it has been "sent" to a small group, but the public invite has not happened


---

# Waiting on you (the whole list, both parts)

3. **RocketForge screenshots**: move to Dropbox reference (recommended) or commit?
- move
4. **Phase-2 301** (old subdomain → mmrsim): say when — ~2 weeks or once your invitees
   confirm they've moved (mechanics: rename plan Task 10).
   - all invitees have confirmed they have moved - so we can complete this item now
5. **Clocking**: stands at WARN — one branch to flip if you disagree. - keep at WARN
6. **Full multi-config .ork import** (M) — want it scoped?
- yes, scope it
7. **Mass-component type picker** (S) — altimeter/tracker identity for components
   created in-app.
   - build it
11. **Site repo, next session there**: act on `chrome-ref-band-findings.md`; retake the
    tools-page screenshot; two internal code comments there still carry the old name.
	- let me know what you need me to do, or just fix it