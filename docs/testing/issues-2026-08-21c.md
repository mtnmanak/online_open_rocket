# OpenRocket Online — Notes & Feature Requests
**Date:** 21 August 2026 

- **S1 — rocket as hero (desktop):** 
- I like your picks for chip contents and this looks slick - keep it and default the drawer closed
- **S2 — callouts on today's layout:** 
- i like it - the callouts to the side are much, much clearer and reduce the clutter inside the rocket
- take a look at the 3D model - RocketForge also uses callouts in 3D and it looks slick. I put a screenshot in the docs folder called "rocket_forge_3d_screenshot.png"
- **S5 — selective richness:** 
- yes - this looks better! i like the motor label in the mount too


## S4 — thought through backwards from the field, per your steer

- Build it

## Shipped this session as v0.048

1. **S3 — first-run tour.** 
- Good first try:
- I think the "Tour" button should live outside the guide - maybe in the menu at the top or in the footer
- Right now, the tour coloring (orange button) blends into the whole decor of the app, so it is not readily apparent that the callout is even their - it just looks like part of the menus. The tour pop-ups should be a distinctly different color/shading so they standout during the tour and draw the user's attention immediately. You could even slightly gray out the rest of the page and focus only on the tour's focus point for that step.


## The rest of the list

- **RocketForge screenshots** — moved to
  `Dropbox\online_open_rocket_reference\RocketForge Screenshots\` (six PNGs), repo tree
  clean. Same rule as the RASAero PDFs.
- **Clocking** — stays WARN, per your call. Nothing changed; the flip remains one
  branch if you ever reverse it.
- **Multi-config .ork import** — 
- if you are ready, start the stages for this


Other issue:
- right now the "unload motors" button is extremely difficult to find - it is a very small triangle next to the motor designation. I think that needs to have its own button and be clearly labeled.
