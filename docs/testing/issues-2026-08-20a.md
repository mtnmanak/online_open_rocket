# OpenRocket Online — Notes & Feature Requests
**Date:** 20 August 2026 

## 1. Where the project stands


  **The beta invite (`docs/beta-invite.md`) is finished copy that was never sent.** 
  - I did send this out to a number of people and I have gotten feedback that people are using it, but I have not received any issues feedback from users yet. I plan to go to a public beta via the rocketry forum next week. please review the beta invite again for any discrepancies and revise if necessary.

---

## 2. Recommended next batch: the beta-launch bundle

Three things outside the running app block it, plus one in-app fix that belongs in the same batch because the invite aims testers straight at it.
- fix those three things so i can expand the beta invite next week

### 2.1 The source repo is still PRIVATE, with Issues enabled

- Go public on the source repo and turn off issues. The landing page on GitHub should point users to the single feedback page for the website.

### 2.2 The five uncommitted docs need to push

- push everything that can be pushes before completing the work to go public on GitHub

### 2.3 The invite's first claim is false — cut it or build it *(your call — §3.1)*

- while revising the beta invite, as per my instructions above, fix this issue - add the link

### 2.4 Phone-width header overflow — fix before testers arrive on phones *(treatment: §3.2)*

- Fix it

---

## 3. Waiting on you — the consolidated list

Everything currently gated on a decision of yours, in one place (several carried over from
response-2026-08-12b so your next issues file can adjudicate them all at once):

1. **"Designs that open from a link"** — cut the phrase from the invite, or scope it as a
   feature in the beta batch (§2.3).
2. **Header overflow treatment** — wrap to multiple rows (with dropdowns re-anchored), or
   the site band's own pattern (`overflow-x:auto` + `min-width:0`, horizontal scroll,
   `styles.css:392-418`), which sidesteps the dropdown problem entirely (§2.4).
3. **Go public** — your call and your sitting; Issues off in the same breath (§2.1).
4. **Three nav-band findings that belong UPSTREAM in the site repo's `chrome.ref.js`**
- give me a blurb in a file i can use in the sessions for the other tools to describe exactly what they need to do
5. **Clocking: warn or refuse?** 
- explain the necessary fix for this
6. **One click on the 3D 📷 with "Fit rocket to frame" on** 
- I clicked the "fit rocket" button and it worked fine
7. **Footer GitHub-link exception vs chrome-spec MUST 6** — 
- I am not sure what you need to do for this - can you just fix it?
8. **Supersonic aero default-ON flip** — 
- we do not have enough feedback yet to determine if we can flip this switch yet, keep it queued for later
9. **LICENSE file** — 
- since i decided to go public, decide what the best course of action is for this and do it - if you need my approval, ask
10. **Design optimization** 
- we can queue this for after getting more beta feedback

---

### 4.1 `.ork` round-trip data loss (M — no engine rebuild, no decision needed)

- can you just fix this issue now? What is blocking you from implementing the fix?

### 4.2 One engine rebuild, four queued kernel fixes (L — needs a JDK on this machine)

- again, can you fix this now or is there a decision you need?

### 4.3 User guide is a version behind, and the "documentation workflow" is fiction (S)

- update the guide and deploy

### 4.4 Two silent-failure defects on the localStorage quota path (S)

- fix it

---

## 5. Housekeeping (no decision needed — next session should just do these)

1. **`npm install` at the repo root.** 
- do it
   
2. **Fix CLAUDE.md's machine paths** (folded into the already-dirty file): repo is at
- fix all this and standardize the fact that I use two machines (my desktop and laptop) and the repo lives under e:\git on my desktop and c:\git on my laptop. scrub everything and make sure there are no longer any stray references to repo locations I no longer use. Another task - has the repo gotten clogged with old files? Should you go through and archive anything old in the repo to make things clean and then gitignore that folder? Do some housekeeping here to keep things clean.
- Also - we should be removing all references, mechanisms, pointers, mention, etc to the dev pages - scrub for that as well.
3. **Add `engine-java/`, `validation/`, `scripts/` to CLAUDE.md's Architecture list.**
- fix it

4. **Neutralize the `deploy-pages.yml` runbook header** (
- fix it