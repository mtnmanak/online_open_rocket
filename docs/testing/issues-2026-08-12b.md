# OpenRocket Online — Notes & Feature Requests
**Date:** 12 August 2026 

## JOB 1 — the Nav Contract

Two fixes, and the first one is live right now in production.
▎
▎ (a) Your contract fetch points at the wrong host. Your bundle fetches https://mountainmanrockets.pages.dev/chrome/nav.v1.json. You copied that constant out of chrome-spec.md §5 when the spec's literal was still pre-cutover — the spec has been corrected. Repoint the CONTRACT constant in your useMmrNav hook to https://www.mountainmanrockets.com/chrome/nav.v1.json. It works today only because the preview alias still answers 200; if that alias is ever renamed or retired, your band silently freezes on its baked fallback and nothing reports it. Also grep your bundle for any other pages.dev in a URL. (Your fallback nav links were checked and are already on www — it's just the contract constant.)
▎
▎ (b) The band wraps to 2–3 rows between 768px and ~924px. Your nav is flex-wrap:wrap with overflow:visible, the shape that wraps. Apply exactly the CSS block below — base selectors, not a media query:
▎ .mmr-band        { flex-wrap: nowrap; }
▎ .mmr-band-brand  { flex: none; }
▎ .mmr-band-search { flex: none; }
▎ .mmr-band-nav    { flex-wrap: nowrap; overflow-x: auto;
▎                    -webkit-overflow-scrolling: touch; scrollbar-width: none;
▎                    margin: -4px 0; padding: 4px 0; }   /* focus-ring gutter */
▎ .mmr-band-nav::-webkit-scrollbar { display: none; }
▎ .mmr-band-nav li { flex: none; }
▎ Only this stays in @media (max-width:767px):
▎ .mmr-band { gap: .6em; }
▎ .mmr-band-search form { display: none; }
▎ .mmr-band-search-link { display: inline; font-size: 1.05em; }
▎ The nav is the only part that gives — overflow-x:auto zeroes its flex minimum size so it scrolls while brand and search hold width. Don't raise the breakpoint instead; ~924px is derived from today's eight labels and rots the moment a ninth is added. The -4px/4px pair is a focus-ring gutter, not spacing: a scroll container clips at its padding edge and Chrome's 3px ring was being shaved. Keep both halves.
▎
▎ Your Workbox precache is untouched — the contract fetch is runtime-only and cross-origin. PWA offline-test before pushing, and confirm the offline band still renders your baked fallback.
▎
▎ Verify: ~1000px → 700px stays one row (34px/29px) · no overflow at 375px · tab in, focus ring intact · DevTools Network shows the contract fetched from www, and the band's data-mmr-source reads live. Bump version.json, push, tell Eric the version.

### The component STL exporter

build it

### DXF — done



### STEP / CNC — your scope-narrowing question

I agree with your recommendations - let's put fabrication geometry and STEP files in the backlog for now and build it out later



### The Darkstar image

yes, give me either the "auto-fit before snapshot"** or a trim-to-content pass on export, whichever makes more sense

---

GitHub-link exception in the footer - looks good for now