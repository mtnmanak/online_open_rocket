# OpenRocket Online — Notes & Feature Requests
**Date:** 21 August 2026 


## S1 — rocket as hero (desktop): built as mocked

- this looks good, with a real issue - when the rocket is flipped 90 degrees (vertical orientation) and the "all stats" drawer is open, the stats cover the bottom of the rocket and there is no way to zoom out or move the rocket so you can see it. Need to deconflict the collision between the stats drawer and the rocket view

## S2 in 3D — your screenshot, adapted

- this looks good, couple suggestions:
- The CG/CP balls in the rocket are pretty big - maybe too big, they kind of overwhelm the rocket.
- the whole 3D view needs a "reset view" button or something like that. I was able to inadvertantly zoom too far in and move the rocket way out of view - it was very difficult to get the rocket back in view. In addition to a mechanism to reset the view, it would be nice to have a few buttons for some preset 3D views like side and aft.
- we still don't have any real visual shading in 3D view to see internal parts. The rocketforge 3D view is visually quite nice. I feel like we aren't comparable to their 3D view yet.

## Multi-config .ork — Stage A is in

- this is an improvement and i like that all the configs are loaded in the backend and ready for use, but have also been wondering if rockets should just be loaded into the design window with no motor loaded. The user can stil see all the motor configs (and load them) on the motors & launch tab and, once they load a motor, it should stay loaded until they unload it or change it, but I am trying to find a good reason why we start a session with a motor loaded?
**Stage B** 
- I am not sure if the answer to my question above affects Stage B, if not, then let's build it


2. **The two invite copy calls** 
- i don't think we need two - move on from the old sub domain and let's just proceed with the new one.
3. **Multi-config Stage B** — 
- is there any good reason to wait?
