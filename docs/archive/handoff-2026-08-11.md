# Session handoff — 2026-08-11 (v0.041 → v0.043, issue batches a + b + adjudication)

For the next Claude session. Read this first; per-version detail lives in
`docs/working-notes.md`, item-by-item batch status in
`docs/testing/response-2026-08-11a.md` and `response-2026-08-11b.md`.

## ⚠ THE ISSUE-TRACKER QUESTION IS ADJUDICATED — do not re-derive it

Parallel sessions across Eric's repos had been independently designing the
shared feedback tracker; he closed everything and adjudicated centrally. The
single source of truth is
**`E:\git\mountainmanrockets\docs\issue-tracking-consolidation.md` (rev. 3,
ADJUDICATED)** — read it before touching anything tracker-related. Summary:

- **One public tracker: `mountainmanrockets-feedback`** (the renamed
  site-feedback repo) for the site + the four public tools. No second
  tracker, ever, without the §3 graduation trigger. Diesel Dashboard is out
  by standing ruling; Rocket Locator keeps its own Issues tab.
- Our `docs/feedback-repo-kit/` files were **adopted** as the tracker's
  source (label spellings won) with central edits (`blank_issues_enabled:
  false`, extra contact links, NO `type:*` labels). The tracker setup/labels/
  site links are done **centrally from the mountainmanrockets repo — never
  from here**. SETUP.md here carries a ⛔ superseded banner; keep it.
- **This repo's assigned scope is DONE (v0.043)**: the in-app 🐞 Feedback
  menu + guide section (wired to the tracker with template/tool/version
  prefilled, mailto fallback, new-tab/mailto rulings honored) and the repo
  README with the tracker line.
- **Still open for Eric** (adjudication §9, not ours to start): the Rocket
  Locator call, the `/online_tools/` door, and — the one item for THIS repo —
  **no LICENSE file** despite being a publicly deployed GPL-3.0-or-later
  derivative (`docs/deployment.md:105-110`). Raise it, don't unilaterally
  add it: license-file wording/source-offer mechanics are his call.

## One-line state

**v0.043 LIVE and verified** (2026-08-11). 270 tests green (247 app +
23 engine). Tree clean, local == origin/main. **Deploy = `git push` to main**
(GitHub Actions → Cloudflare Pages, ~90 s;
https://online-open-rocket.pages.dev). Next version **v0.044**.

## What shipped today (two releases, one day)

- **v0.041 (batch a)**: nose/transition shapes now DRAW correctly in 2D+3D
  (kernel-exact `src/tree/shapeProfile.ts`, incl. clipped transitions +
  shape parameter, which gained an editor field); component data → CSV/XLSX;
  2D SVG (prints 100 % scale) + image exports with data header; repo-move
  fixes (broken npm workspace junctions, dead-path orphan patch).
- **v0.042 (batch b)**: 🖨 **per-component watertight printable STL** (mm)
  for 13 component types (`src/tree/solidMesh.ts` — manifold-proven, built
  via a 5-agent workflow whose adversarial verify caught 2 real geometry
  defects pre-release); whole-rocket `.stl` (reference) and `.glb` with
  colors in Save/Export; ⬇/📷 **Image pickers** (PNG/JPG × HD/4K/8K; the 3D
  path re-renders at export width); guide shape-parameter table + formats.
- **v0.043 (post-adjudication)**: header **🐞 Feedback menu** (bug/feature
  → tracker issue forms with tool+version prefilled, browse-issues, mailto)
  + guide "Feedback & Bug Reports" section (both mirrors) + the repo's
  first **README.md** (with the tracker line). New `bug` icon in Icon.tsx.

**Every batch-b smoke item is confirmed**, including the 3D 📷 button: Eric
exported his Darkstar 3" at genuine 8K on live v0.042 — the proof sits at
`docs/2D_3D_Models/Wildman_Darkstar_3_-3d.png` (7680×4649, correct header).

## Waiting on Eric (queue these when sessions reopen)

1. **DXF outlines for flat parts** (fins/rings/bulkheads — the CNC quick
   win) — recommended in response-b §6, awaiting go/no-go.
2. **STEP via OpenCascade-WASM** (lazy chunk, own build session, Eric
   validates in Fusion/SolidWorks) — awaiting go/no-go in principle.
3. **Slicer pass** on a 🖨 component STL (nose cone / centering ring) —
   requested, not yet reported.
4. From v0.040: declined shroud-conversion prompt re-asks every import.
5. `docs/testing/issues-2026-08-11c.md` currently contains **no new issues**
   — it's a verbatim copy of response-b with an issues header (his
   annotation template, most likely). **Check it for added annotations
   before treating it as processed.**
6. **LICENSE file** — flagged in the adjudication (§9.5) as this repo's open
   item; raise with Eric, his call on wording/timing.
7. The 🐞 links assume the central tracker setup (rename + templates +
   labels) has landed — it was in progress centrally at session close. If a
   link 404s, that setup simply hasn't finished; do NOT "fix" it from here.

## Queued kernel work (next engine rebuild — full ritual required)

- Bridge ignores `shapeParameter` on TRANSITIONS (nose cones fine) —
  3-line ComponentFactory fix.
- Imported `<shapeclipped>false</shapeclipped>` can't be honored (we export
  the kernel-true value since v0.041; import-side needs the bridge).
- Ritual: JAVA_HOME → portable JDK (`C:\Users\Eric\.online-openrocket\
  jdk-17.0.19+10`), `gradlew.bat generateJavaScript` in engine-java/,
  `node engine-java/scripts/build-engine.mjs`, difftest ×3 **AND** engine
  vitest (difftest alone can miss tree-API bugs).

## Resume commands

```
cd E:\git\online_open_rocket    # moved from G:\git 2026-08-11
git fetch && git status         # Eric works desktop + laptop — verify sync FIRST
npm install                     # ONLY if module-resolution errors (junctions break on copy)
npm test --workspaces           # 270 tests
npm run build                   # engine tsc + app vite → packages/app/dist
```

## Session gotchas (hard-won today)

- **Session store is localStorage** (`online-openrocket.session.v1`), not
  sessionStorage — browser-test protocol: back up the key, restore + remove
  backup after.
- **Do NOT monkeypatch rAF→setTimeout while testing the 3D view** — it
  breaks R3F mounting. (Still fine for driving sims per older notes.)
- The CDP-driven Chrome intermittently **wedges WebGL** (R3F canvas stuck at
  300×150, `THREE Context Lost`) — environmental, hits the live site too;
  verify 3D by unit tests + Eric's click, don't rabbit-hole.
- Repeated 2D↔3D toggles under CDP leak WebGL contexts.
- CDP screenshots time out once then succeed — retry, don't debug.
- STL/solids: the m→mm ×1000 lives ONLY in `services/stlExport.ts`;
  `solidMesh.ts` is pure SI like everything else.
- carve.mjs FAILS on orphaned patch files — don't recreate
  `engine-java/patches/rocketcomponent/` (the dead path).
- Non-ASCII files: Edit/Write tools only, never PowerShell pipes.
