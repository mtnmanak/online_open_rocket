# docs/archive

Superseded documents kept for history — do not follow them as instructions.
Session handoffs here are replaced by the newest `docs/handoff-*.md` at top level;
`deployment.md` is replaced by CLAUDE.md's "Deploying" section (Cloudflare Pages);
`feedback-repo-kit/` was adopted into `mtnmanak/mountainmanrockets-feedback` (see `docs/feedback-tracker.md`).
`handoff-2026-08-12.md` is replaced by the newer `docs/handoff-2026-08-20.md` and
`docs/handoff-2026-08-21.md`; `staging-clusters-design.md` and
`pods-implementation-plan.md` are design docs for features that have since fully
shipped (see the changelog in `docs/working-notes.md`).
