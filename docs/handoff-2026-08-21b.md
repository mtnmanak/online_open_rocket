# Session handoff — 21 August 2026, second sitting (RENAME SHIPPED as v0.047; issue batch 08-21a answered; S1–S5 design proposals await Eric)

Self-contained. Read this before `working-notes.md`'s per-version history. Supersedes
`docs/handoff-2026-08-21.md` (the same-day naming-brainstorm sitting — its rename AUDIT
sections remain the phase-2 reference; everything else there is done).

## One-line state

**The product is now MMRocket Sim, live and verified at
https://mmrsim.mountainmanrockets.com (v0.047), repo renamed to
`mtnmanak/mmrocket-sim`** — and issue batch 2026-08-21a (both versions of it, see below)
is fully answered in `docs/testing/response-2026-08-21a.md`. Tests 26 engine + 562 app
green; last deploy (e9eee61) green and live-verified. Tree clean except
`docs/RocketForge Screenshots/` (untracked ON PURPOSE — Eric decides Dropbox vs commit).

## ⚠️ FIRST on the DESKTOP (E:\git\online_open_rocket)

1. `git remote set-url origin https://github.com/mtnmanak/mmrocket-sim.git` (the GitHub
   repo was renamed; the old URL redirects, but fix it properly).
2. If the desktop never re-synced after the 2026-08-20 history rewrite, follow
   `docs/handoff-2026-08-20.md`'s re-sync section BEFORE anything else.
3. Then the usual `git fetch` + confirm local == origin/main.

## What shipped this session (all pushed, deployed, live-verified)

1. **The v0.047 rename release** — executed subagent-driven from
   `docs/superpowers/plans/2026-08-21-mmrocket-sim-rename.md` (the plan file doubles as
   the execution record):
   - Name decided (MMRocket Sim; TESS clear 2026-08-21; "MMAero Sim" killed in vetting —
     M&M Aerospace owns the token; generic `sim.` subdomain rejected because the family
     already has a separate Motor Simulator tool).
   - All in-app strings, PWA manifest (name/short_name "MMRocket Sim" + new `id: '/'`),
     six export stamps, GPL source link → `mtnmanak/mmrocket-sim`. OpenRocket
     *attribution* untouched everywhere (that's a CLAUDE.md rule now — "Naming rule").
   - Old-origin migration layer: `hostMigration.ts` + `MovedNotice.tsx` — the retired
     `openrocket.*` subdomain serves the same build behind a persistent banner and
     self-dismantles its SW. **No 301 yet — deliberate**: invitees' autosaves are
     origin-bound; the 301 is **phase 2**, mechanics in the plan's Task 10 (no SW
     carve-outs needed; the audit's carve-out idea was superseded — workbox precache
     breaks across 301s anyway, the app-level dismantle replaced it).
   - Externals, all live-verified: Cloudflare domain attached (both subdomains serve);
     feedback tracker dropdown "MMRocket Sim" + label `tool:mmrocket-sim` + workflow +
     README; GitHub repo renamed; main-site `tools.mjs` → MMRocket Sim v0.047 (site
     deployed; its old `/online_open_rocket/` path now 301s STRAIGHT to mmrsim — the
     docs were corrected to say so); guide + README + invite + CLAUDE.md rewritten.
2. **Issue batch 2026-08-21a answered** — NOTE THE TRAP: the issues file was REPLACED on
   disk mid-session (annotated survey → RocketForge note). Caught in review; the
   committed response answers both: **Part 1** = the RocketForge note (screenshots
   studied, five design proposals S1–S5, invite verbiage added, what-not-to-copy);
   **Part 2** = the annotated survey closeout (most items were already closed by
   v0.046/earlier adjudications in `response-2026-08-20b.md`; evidence cited per item).
3. **Batch fixes shipped with it**: guide gains storage-quota + address-bound-autosave
   sentences and a fixed Save/Export list (regenerated `userGuide.ts`); invite gains the
   "Free, open source, and no account — ever" first bullet; rename note added atop
   `chrome-ref-band-findings.md`; `handoff-2026-08-12.md` + `staging-clusters-design.md`
   + `pods-implementation-plan.md` archived into `docs/archive/` (README updated; NO
   gitignore — public history); dead `G:\git` paths in the rasaero research doc →
   repo-relative; README names the LICENSE file; two new regression tests pin the
   export stamps (`creator="MMRocket Sim"`, STL header).

## ⏸️ RESUME POINT — Eric is answering these now

Everything gated on Eric lives in one list: **`docs/testing/response-2026-08-21a.md` →
"Waiting on you"** (12 items). Headliners:

1. **S1–S5 RocketForge-inspired proposals** (rocket-as-hero canvas + floating stats
   chip; CG/CP leader-line callouts; first-run tour; phone vertical-rocket +
   bottom-sheet layout, staged; selective visual richness). Suggested order: S3 + S2
   before the forum post. Offer stands: side-by-side mockups on a design canvas.
2. **Two invite copy calls** (non-affiliation half-clause; keep the migration note in
   the public post?) — then the **public rocketry-forum beta post** (~week of 08-24).
   The invite is otherwise accuracy-checked and ready.
3. **Phase-2 301** old subdomain → mmrsim: fire only when Eric confirms invitees
   migrated (~2 weeks). Rename plan Task 10 has the exact redirect rule.
4. **RocketForge screenshots**: Dropbox reference folder (recommended) or commit.
5. Site-repo session someday: act on `chrome-ref-band-findings.md` (3 band fixes +
   MUST 6 spec amendment), retake the tools-page screenshot.
6. Feature queue unchanged: full multi-config .ork import (M), mass-component type
   picker (S), geodetic selector (S, rides next engine rebuild), supersonic default-ON
   (still blocked on the ARCAS anchor), design optimization (post-beta).

## Facts a future session will want

- Old handoff-2026-08-21's "invite never sent" claim was WRONG — the invite went out on
  the old address; real users sit behind the moved banner. That is why phase 2 waits.
- v0.046 had already shipped the four kernel fixes + .ork round-trip + quota surfacing —
  verified in code with the artifact rebuilt at `1eb1e98`, difftest passed. Don't re-do.
- Clocking stays WARN; footer GitHub `_blank` exception STANDS (both adjudicated in
  `response-2026-08-20b.md`, recorded in working-notes + feedback-tracker rulings).
- RocketForge (rocketforge.space) is a friendly peer, not a rival — Eric's framing, and
  he keeps the OpenRocket developers informed as a courtesy. Mind that tone in copy.

## Verification / environment

- 26 engine + 562 app tests green this session; `npm run build` clean; deploy workflow
  green at e9eee61; live checks: new host v0.047 + manifest id, old host banner +
  restored autosave, pages.dev redirect preserving hash, site tools page in sync
  (`check-tools.mjs` 0 problems).
- No background jobs, workflows, or worktrees left running. Laptop tree clean except the
  screenshots folder (deliberate).
