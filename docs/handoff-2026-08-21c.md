# Session handoff — 21 August 2026, third sitting (BATCH 08-21b EXECUTED: v0.048 shipped, phase-2 301 LIVE, mockups await Eric)

Self-contained. Read this before `working-notes.md`'s per-version history. Supersedes
`docs/handoff-2026-08-21b.md` (rename shipped as v0.047; its desktop re-sync note is
folded in below).

## One-line state

**v0.048 is live and verified at https://mmrsim.mountainmanrockets.com** (first-run
tour S3, CG/CP leader-line callouts S2, mass-component type picker), **the phase-2 301
is live** (old `openrocket.*` subdomain → mmrsim, path+query preserved, curl-verified),
the site repo shipped its band-findings fixes + v0.048 card, and everything Eric
green-lit in `docs/testing/issues-2026-08-21b.md` is done — his new, much shorter
"Waiting on you" list is at the end of `docs/testing/response-2026-08-21b.md`.

## ⚠️ FIRST on the DESKTOP (E:\git\online_open_rocket)

1. `git remote set-url origin https://github.com/mtnmanak/mmrocket-sim.git` (still
   pending from the repo rename if not yet done there).
2. Then the usual `git fetch` + confirm local == origin/main.

## What shipped this session (all pushed, deployed, live-verified)

1. **v0.048** (`5310baf` + docs `734f4ec`):
   - **S3 first-run tour** — `components/FirstRunTour.tsx`: six `data-tour`-anchored
     stops (tree → canvas → Motors & Launch → Launch → Results → Guide/Feedback), shows
     once (`online-openrocket.tour.v1` = 'done'; Skip counts), suppressed for share
     links and restored sessions (`shouldAutoStartTour`, pure + tested), replay via
     Guide's ⟲ Tour, opt-out in Preferences → Display (`prefs.tourOff`). Live-walked
     end-to-end in a real browser post-deploy: all six steps, tab switching, flag set,
     no re-show on reload.
   - **S2 callouts** — `TreeSchematic.tsx`: dashed leaders from CG/CP markers to
     labeled dots in reserved lanes (exported `calloutLayout` helper + `CALLOUT_LANES`
     height reservation), margin text color-coded via `stabilityState`
     (`"✓ 1.52 cal — ok"` vocabulary from SimResults), CP ink now
     `var(--status-serious)`, and the three status vars baked into `EXPORT_VARS`
     (`schematicExport.ts`) so SVG/PNG exports keep their colors.
   - **Mass-component Type picker** — one options FieldDef in `tree/schema.ts`
     (8 desktop enum values, `masscomponent` first = sparse default free); PropertyPanel,
     component table and CSV picked it up generically; `.ork` round-trip already existed.
   - 26 new tests (13 TreeSchematic, 3 masstype, 10 tour) → 26 engine + 588 app green;
     tsc clean; guide updated in 5 places and regenerated.
2. **Phase-2 301 EXECUTED** — Eric's trigger ("all invitees have confirmed"). Cloudflare
   zone Single Redirect built in the dashboard exactly per rename-plan Task 10 (rule
   "openrocket subdomain 301 to mmrsim (rename phase 2)", placed after the root→WWW
   rule). Verified: root and `/some/path?x=1&y=2` both 301 with exact Location;
   canonical still 200. Task 10 marked executed in the plan; CLAUDE.md deploy table +
   invite migration/maintainer notes rewritten to the post-301 reality.
   `hostMigration.ts`/`MovedNotice` remain in-app but unreachable (removal = optional
   tidy-up, no urgency; phase 3 domain-detach stays optional/months away).
3. **RocketForge screenshots** moved to
   `C:\Users\peltz\Dropbox\online_open_rocket_reference\RocketForge Screenshots\`.
4. **Multi-config .ork import SCOPED** — `docs/multi-config-import-scope.md` (research
   verified against code + 24.12 source): Stage A = read-all-configs-correctly + pick
   at import (M, recommended alone — today's import can silently pair a kept motor
   with another config's ignition override, and `<deploymentconfiguration>` is
   invisible both ways); Stage B = named configs app state (M–L); Stage C = full parity,
   gated on the next engine rebuild.
5. **Site repo** (`C:\git\mountainmanrockets`, 4 commits pushed, deployed,
   check-tools "all in sync"): chrome.ref.js **1.3.0** (focus-ring gutter 4-sided;
   768–924px overflow edge-fade — conservative option 1; phone gap 8px), chrome-spec
   MUST 6 gains the GitHub `_blank` exception explicitly, last two old-name comments
   fixed, tools card screenshot retaken at v0.048 (tour pre-dismissed via its flag so
   it doesn't photobomb; scrollbar hidden; 1200×770 as before), tools.mjs fallback
   0.048.
6. **Mockups for Eric** — design canvas
   https://claude.ai/code/artifact/1b9fe671-771e-4d48-8bcf-f537315f63f1 : S1
   rocket-as-hero desktop (floating chip + drawer tweak toggle), S2 on today's layout
   (shipped), **S4 reframed launch-centered** (phone home = Fly screen — apogee/
   optimum delay/descent/max-v, motor swap, three field launch conditions, thumb-zone
   LAUNCH, bottom bar Design/Fly/Results) per Eric's working-backwards steer, S5
   selective richness (motor tint+label, hover highlight, 3D pass sketch). Sticky
   notes carry the open questions.
7. `chrome-ref-band-findings.md` archived (its archive condition — "site repo acts on
   it" — is met); archive README updated.

## ⏸️ RESUME POINT — waiting on Eric (the new short list)

See `response-2026-08-21b.md` "Waiting on you": S1/S5/S4-direction verdicts off the
canvas; the two invite copy calls (still gating the ~08-24 forum post; the 301 made the
migration-note call mostly moot); multi-config Stage A go/no-go. Standing queue
unchanged: geodetic selector (next engine rebuild), supersonic default-ON (ARCAS
anchor), design optimization (post-beta).

## Follow-ups queued for future sessions (no Eric input needed)

- **Band ref 1.3.0 re-translation into THIS repo's `styles.css`** (the React-adapter
  copy): the three 1.3.0 rulings now exist upstream; this repo's band still carries
  the pre-1.3.0 CSS. Small, needs the spec §5 re-translation traps + width-sweep
  verification. Ship with the next real release.
- **bp_calculator + motor_dashboard re-vendor** of chrome.ref.js 1.3.0 (their own
  repos; site check-tools already flags bp_calculator — the signal working as
  designed).
- Optional tidy-up someday: remove `hostMigration.ts`/`MovedNotice` (dead since the
  301) and their tests.

## Facts a future session will want

- **Beta-send clarification (Eric, this batch):** the invite went to SELECTED
  individuals — not a public send. The public rocketry-forum post is the still-pending
  step. Don't re-derive "was the invite sent" from older handoffs (08-21's "never
  sent" claim was already corrected; this nails the scope).
- Clocking stays WARN — re-confirmed by Eric in this batch (third confirmation;
  standing adjudication in `response-2026-08-20b.md` §3.5).
- The tour deliberately does NOT show for restored-session users — Eric's "auto-shut
  off after first use" read as "existing users have already used the tool". Replay
  covers them.
- Cloudflare dashboard work happens via Eric's logged-in Chrome (claude-in-chrome);
  wrangler is NOT authenticated on the laptop, but the SITE repo's `.env` carries
  `CLOUDFLARE_API_TOKEN` for its `npm run deploy` (used this session; never echo it).
- The design-canvas mockup sources live in this session's scratchpad
  (`scratchpad/mockups/*.dc.html`) — a future session editing the canvas should
  WebFetch + `--extract` from the artifact instead (scratchpads are session-scoped).

## Verification / environment

- 26 engine + 588 app tests green; `npm run build` clean; version gate "0.048 is
  consistent"; deploy workflow green; live checks: version.json 0.048 on the canonical
  host, tour full walk + no-reshow, callout text + dashed leaders present in live DOM,
  old subdomain 301s (root + path+query), site tools page v0.048 + new screenshot,
  site check-tools "all tools reachable and in sync".
- No background jobs, workflows, or worktrees left running. Both repos' trees clean
  after the final push (this handoff's commit is the only thing after `734f4ec`).
